# Riora OS — Claude Code 運用ルール

## v1.0 は正式凍結済み（2026-07-03）

スタッフアプリ v1.0 は `docs/V1_0_FREEZE_DECLARATION.md` により正式に凍結されている。
凍結中は以下を**一切禁止**する（バグ修正の提案・確認は歓迎するが、コード変更は不可）：

- 機能追加
- 仕様変更（5タブ構成・今日タブのブリーフィング仕様・TL-5構成・AI提案の会話トーン等）
- `docs/V1_0_SNAPSHOT.md` に記載のv1.0スコープ外ファイルの修正
- LINE領域（`src/components/line/**` 等）の変更

既知バグA（LINE反応率固定値）・B（音声メモ許可タイムアウトのタイマーリーク）は
`docs/V1_0_KNOWN_ISSUES.md` に記録済み。**v1.0.1以降で対応**する（今は直さない）。

凍結解除・v1.0.1着手の指示があった場合のみ、上記の禁止を解いてよい。

### v1.0.1 着手済み事項（今日タブのブリーフィング仕様のみ・2026-07-03ユーザー承認）

`docs/architecture/Riora_ブリーフィング画面イメージ.html`のデザインに基づき、今日タブの
ブリーフィング仕様（次のお客様表示・3行注意事項・折りたたみ詳細・このあとの予約リスト）を
v1.0.1として変更した。**この解除は今日タブのブリーフィング仕様に限る。** 5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域については引き続き凍結を継続する。

### v1.0.1 着手済み事項（顧客タブ・サブスク契約経験バッジのみ・2026-09-14ユーザー承認）

サブスク決済分離Phase 1/2（is_subsciber/subscribed_atの再配線）に伴い、顧客タブ
（`CustomersScreen.tsx`等）に「サブスク契約経験あり」バッジ（事実表示・評価系UIではない）を
追加するため、顧客タブに限り凍結を解除した。**この解除は当該バッジ表示に限る。** 5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域については引き続き凍結を継続する。

### v1.0.1 着手済み事項（Salon Boardカルテ取込・スマホ写真カルテの削除のみ・2026-09-14ユーザー承認）

方針転換（過去カルテはカルテメモへ直接入力する運用に統一・写真撮影はiPadへ一本化）に伴い、
以下2点の機能削除に限り顧客タブ・CustomerBottomSheet.tsx・iPadカルテ画面の凍結を解除した。

- Salon Boardカルテ取込の導線削除: `CustomerBottomSheet.tsx`・`IpadStaffKarteView.tsx`の
  カルテ取込セクション、`CustomersScreen.tsx`の「📋カルテ取込」ボタン(iPadカルテ検索への
  近道。CustomerBottomSheet.tsx側の別のiPadカルテ検索導線は削除していない)。
  `KarteImportSection.tsx`・関連API(analyze/commit)・`karte_imports`テーブルは
  呼び出し元が無くなるだけで削除していない(将来の再利用に備えて保全)。
- スマホの写真カルテ機能(撮影する/選択して追加/Before-After比較)を閲覧機能も含めて
  完全削除。`PhotoTimelineView.tsx`・`PhotoCaptureView.tsx`・`PhotoLibraryPickerView.tsx`・
  `PhotoComparisonView.tsx`・`batchUpload.ts`は削除。`usePhotoCapture.ts`・`captureFrame.ts`・
  `constants.ts`(写真カルテ「原本保存化Phase A」の未コミット作業を含む)はユーザー判断により
  ファイルとして保全(使用箇所は削除済み)。iPad側の写真表示機能(`PhotoCompareKit.tsx`・
  `ipadKarteData.ts`経由、共有部分は`photoApiClient.ts`のみ)には影響していない。

**この解除は上記2点の機能削除に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・
admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（顧客タブ一覧の簡略化のみ・2026-09-14ユーザー承認）

全スタッフが全顧客を閲覧できる方針のため、`CustomersScreen.tsx`から以下3点を削除する
限りで顧客タブの凍結を解除した。

- 「私のお客様/全顧客」タブ: 常に全顧客を表示する固定仕様にした(元々API側は常に
  アクセス可能な全顧客を返しており、タブはクライアント側filter()のみだったため
  API・データ取得への変更はない)。
- 「来店日順」「売上順」のソート切替: 削除し、来店日順(直近来店が新しい順)固定にした
  (旧デフォルトと同じ並び順のため、見た目上の挙動変化はない)。
- 名前の下の区分ラベル(効果重視型・信頼構築型等)の表示: 削除した(検索対象データとしての
  タイプ一致検索は維持)。

**この解除は上記3点の削除に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・
admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（CustomerBottomSheet.tsx「🔍カルテ検索・取込」ボタン削除のみ・2026-09-15ユーザー承認）

commit 46c6900で削除したSalon Boardカルテ取込セクションとは別に、CustomerBottomSheet.tsx
シート最上端に単独配置されていたiPadカルテ検索導線(PHASE IPAD-2・「🔍 カルテ検索・取込」
ボタン、`showIpadKarteSearch`state、`IpadKarteSearchView`の呼び出し)を削除した。
`IpadKarteSearchView.tsx`自体は他の呼び出し元が無いことを確認した上でファイルとして保全
(将来の再利用に備え削除しない)。

**この解除は当該ボタンおよびその呼び出し導線の削除に限る。** 5タブ構成・TL-5構成・
AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様
については引き続き凍結を継続する。

### v1.0.1 着手済み事項（スタッフカルテ「目標」表示・編集UIの削除のみ・2026-09-15ユーザー承認）

お客様モードに続き、スタッフカルテ（iPad・スマホ）側の「目標」欄も不要と方針確定したため、
以下の表示・編集UIに限り顧客タブ・CustomerBottomSheet.tsx・iPadカルテ画面の凍結を解除した。

- `IpadStaffKarteView.tsx`から`GoalEditCard`の呼び出し（import・JSX・関連コメント）を削除。
- `CustomerBottomSheet.tsx`から`GoalSection`の呼び出し（import・ErrorBoundaryラップ含む
  JSX）を削除。
- `GoalSection.tsx`・`GoalEditCard.tsx`のファイル自体、GET/PATCH `/api/customers/[id]/goal`
  API、`brain_customers.goal_note`列・既存データはいずれも削除せず保全（呼び出し元が
  無くなるのみ）。`ipadKarteData.ts`のgoalNote取得ロジック自体にも手を加えていない
  （表示先が無くなり未使用データになるが、データ取得層はスコープ外のため無変更）。

**この解除は上記UI削除に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・
admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（IpadStaffKarteView.tsx「前回/今回」写真表示のobjectFit修正のみ・2026-09-15ユーザー承認）

お客様モード側の写真見切れ修正（aspectRatio調整）だけでは解消せず、objectFit:coverである
限り写真とコンテナの比率が一致しない場合に必ずどこかが切り抜かれる構造的な問題と判明した。
iPadスタッフカルテ側の「前回/今回」写真表示でも同じ見切れが確認されたため、共有コンポーネント
`PhotoPanel`（`src/components/customer/shared/PhotoCompareKit.tsx`）のobjectFitを
`cover`から`contain`へ変更する範囲に限り、iPadカルテ画面の凍結を解除した。

- `PhotoPanel`はCustomerModeView.tsx（お客様モード）・IpadStaffKarteView.tsx（iPadスタッフ
  カルテ「前回/今回」）の両方から使われる共有部品のため、この1箇所の変更で両画面に適用される。
- containへの変更により、写真とコンテナの比率が一致しない場合に上下または左右へ余白が
  生まれる可能性があるが、「写真全体が見切れないこと」をユーザー指示により優先する。
- `EnlargedPhotoPanel`（拡大モード・お客様モードのみ）は元々objectFit:containだったため
  変更不要だった。

**この解除は上記objectFit変更（およびそれに伴うコメント更新）に限る。** 5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ
画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（IpadStaffKarteView.tsx「写真カルテ」セクションの列移動のみ・2026-09-15ユーザー承認）

右カラムに「顧客ステータス・写真カルテ・肌の特徴・今日の施術・今回のホームケア・次回の目安」
が縦に集中して画面が長くなりすぎていた一方、左カラムは「重要事項（該当時のみ）・カルテメモ」
のみで余裕があったため、「📷 写真カルテ（前回｜今回）」セクションを右カラムから左カラムへ
移動する範囲に限り、iPadカルテ画面の凍結を解除した。

- ロジック・見た目（角度タブ・PhotoPanel・onExpand等）自体は無変更、DOM上の位置のみの変更。
- 左カラムの並び順: 重要事項（該当時のみ）→ 写真カルテ → カルテメモ。
- 右カラムに残る「顧客ステータス・肌の特徴・今日の施術・今回のホームケア・次回の目安」の
  並び順は現状維持（写真カルテを抜いただけ）。

**この解除は上記セクション移動（およびファイル冒頭のレイアウト説明コメント更新）に限る。**
5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/
iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（顧客ステータスパネル「店販購入ステータス」のアコーディオン化のみ・2026-09-15ユーザー承認）

「店販購入ステータス（商品ごと）」が商品数分だけ縦に伸びる問題を解消するため、
`CustomerStatusPanel`（IpadStaffKarteView.tsx内）の当該セクションに限りアコーディオン化した。

- デフォルト折りたたみ。見出しに商品件数（例:「店販購入ステータス(商品ごと・12件)」）を表示し、
  折りたたみ時も総数が分かるようにした。
- 上部の「最終来店日／来店回数／来店周期の目安／前回来店から経過／次回来店目安」は
  常時表示のまま無変更。

**この解除は上記アコーディオン化に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・
LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き
凍結を継続する。

### v1.0.1 着手済み事項（IpadStaffKarteView.tsx「写真カルテ」セクションへの撮影・追加機能新設のみ・2026-09-15ユーザー承認）

方針転換（撮影機能のiPad一本化）に伴いスマホ側の撮影導線（commit 46c6900）を削除して以降、
iPad側の撮影導線が未実装のまま残っていた（iPad・スマホどちらからも写真を登録する手段が
存在しない状態）ため、iPadスタッフカルテに撮影・追加機能を新規実装する範囲に限り、
iPadカルテ画面の凍結を解除した。

- 温存されていた`usePhotoCapture.ts`・`captureConfirmFlow.ts`・`captureFrame.ts`・
  `ghostSelection.ts`・`cameraError.ts`・`photoApiClient.ts`（写真カルテ「原本保存化Phase A」
  の土台）はREAD ONLY調査の結果、無傷でそのまま使えると判断し、方針Bどおり流用した
  （旧スマホ実装`PhotoCaptureView.tsx`等の単純移植はしていない・復元もしていない）。
- 新規ファイル`IpadPhotoCaptureModal.tsx`を追加し、「撮影する」「選択して追加」の2つの入り口
  ボタンを`IpadStaffKarteView.tsx`の写真カルテセクション（PhotoPanelのグリッド直下）に追加した。
  既存のPhotoPanel（表示・比較用）自体には一切手を加えていない。
- 部位（正面/斜め/顎/額、`IPAD_KARTE_ANGLES`）の選択を必須化: モーダル内は`bodyPart`未選択
  (空文字)状態で開始し、4つのいずれかを明示的に選ぶまでシャッター/ファイル選択ボタンを
  disabledにする（枚数によらず部位未選択のまま登録できない構造）。
- photoTypeはユーザー確定により常に`'progress'`固定（iPadスタッフカルテの「前回|今回」比較は
  施術前後の区別を表示に使わないため、before/after選択UIは設けていない）。
- `ipadKarteData.ts`に`todayVisitId`（本日来店のvisit_id）の公開、および撮影成功後に写真一覧
  のみを軽量再取得する`refetchPhotos()`を追加した。他の項目（重要事項・目標・顧客ステータス等）
  には触れていない。
- サーバー側API（`POST /api/customers/[id]/photos`）・DBスキーマの変更は不要だった（`bodyPart`
  は元々自由文字列として受理される設計のため）。
- 実機確認（Playwright、本番Supabase・実顧客「小宮山 仁美」）: 部位未選択の間は「写真ライブラリ
  から選ぶ」がdisabledであること、部位選択後にファイルを選ぶと`body_part=face_oblique`・
  `photo_type=progress`で`brain_customer_photos`に保存されること、「撮影する」モーダルも
  エラー無く開けることを確認済み。作成したテストデータは検証後に削除済み。検証用一時spec
  はコミット前に削除。既存ユニットテスト（写真関連118件）に新規の回帰なし。
  **実際のiPad実機カメラでの撮影→保存確認は、スタッフによる実機確認により2026-09-15付けで
  完了済み（カメラ起動・撮影→保存いずれも問題なし、ユーザー報告により確認）。これにより
  本機能は「復旧完了」として扱う。**
- ゴースト（前回写真の半透明重ね表示）機能は今回のスコープ外（2026-09-15ユーザー指示）。
  `usePhotoCapture.ts`側は土台としてそのまま流用しているため内部的には`ghostUrl`等を
  引き続き算出するが、`IpadPhotoCaptureModal.tsx`では意図的に描画しない（未調整のまま
  本番で誤って使われることを避けるため）。正式に作るかどうかは別途相談の上で判断する。

**この解除は上記の撮影・追加機能新設に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・
LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き
凍結を継続する。

### v1.0.1 着手済み事項（写真カルテ角度ラベル再定義＋登録済み写真の削除機能のみ・2026-09-15ユーザー承認）

現場の手書きメモの運用実態（正・右・左・デコ）に合わせ、角度ラベルを「正面・斜め・顎・額」
から「正面・右・左・額」へ再定義した（左右の基準は「写真に写って見えている通りの右・左」）。
併せて、誤って登録した写真を後から削除できる機能を新設する範囲に限り、iPadカルテ画面の
凍結を解除した。

- `IPAD_KARTE_ANGLES`（`ipadKarteData.ts`）・`CUSTOMER_MODE_ANGLES`（`customerModeData.ts`、
  お客様モード）を同時に`[正面, 右, 左, 額]`へ更新した。同じ写真を見る2画面でラベルが
  矛盾しないよう必ず同時変更する方針（PHOTO_LABEL_REALIGN_1と同じパターン）。
- `chin`（顎）は選択肢から削除。本番データ確認（read-only）の結果、`face_oblique`/`chin`/
  `forehead`の実データは0件だったため、データ移行は不要だった。既存の`face_left45`/
  `face_right45`等のレガシーデータはDBの値を書き換えず、`src/lib/photos/bodyParts.ts`の
  `bodyPartLabel()`でレガシー表示ラベルのみ引き続き解決する（新規アップロードの選択肢には
  出さない）。同ファイルは今回新設の削除管理UIで実際に使う形で復活・更新した（従来は
  どこからも呼ばれていない未使用コードだった）。
- 新規`IpadPhotoManageModal.tsx`を追加し、「🗑 写真を削除」ボタンを`IpadStaffKarteView.tsx`
  の写真カルテセクションに追加した。顧客の全写真（過去分含む）をサムネイル一覧＋部位ラベル＋
  撮影日で表示し、個別に削除ボタン→アプリ内確認ステップ（キャンセル可）→確定でソフト
  デリートする。既存の`DELETE /api/customers/[id]/photos/[photoId]`（所有権チェック済み・
  ソフトデリート実装済み）・`deletePhoto()`クライアント関数をそのまま利用し、API・DB変更は
  無し。既存のPhotoPanel（表示・比較用）には一切手を加えていない。
- 撮影直後（プレビュー段階）の破棄は、既存の「撮り直す」（1.5秒タイマー以内）で要件を
  満たすと判断し、追加実装はしていない（タイマー秒数も変更していない）。
- 実機確認（Playwright、本番Supabase・実顧客「小宮山 仁美」）: 角度タブが正面/右/左/額の
  4つで表示され「斜め」「顎」が出ないこと、「写真を削除」ボタンから管理モーダルが開き
  部位ラベル・日付付きで一覧表示されること、削除アイコン→確認ステップ→キャンセルで取り消せる
  こと、確定するとソフトデリート（`deleted_at`設定、行は残存）されることを確認済み。作成した
  テストデータは検証後に削除済み。検証用一時specはコミット前に削除。既存ユニットテスト
  （写真関連103件）・型チェックに新規の回帰なし。

**この解除は上記のラベル再定義・削除機能新設に限る。** 5タブ構成・TL-5構成・AI提案の
会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については
引き続き凍結を継続する。

### v1.0.1 着手済み事項（角度ラベルの文言変更「右→右斜め」「左→左斜め」のみ・2026-09-15ユーザー承認）

分類の考え方（写真に写って見えている通りの右・左）自体は変更せず、表示文言のみ
「右斜め」「左斜め」に変更する範囲に限り、iPadカルテ画面の凍結を解除した。

- `IPAD_KARTE_ANGLES`・`CUSTOMER_MODE_ANGLES`・`bodyParts.ts`の`BODY_PART_OPTIONS`の
  ラベル文言のみ更新した。内部の値(`face_right`/`face_left`等のID)・DB・APIは無変更。
- 実機確認（Playwright、実顧客「小宮山 仁美」）: iPadスタッフカルテ・お客様モードの両方で
  角度タブが「正面・右斜め・左斜め・額」の表記になっていることを確認済み。検証用一時spec
  はコミット前に削除。既存ユニットテスト（写真関連103件）・型チェックに新規の回帰なし。

**この解除は上記の文言変更に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・
admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（撮影ガイド強化 Phase 1（顔の大きさ・位置・傾き検出）のみ・2026-09-15ユーザー承認）

将来のゴースト機能（Phase 2、前回写真を半透明で重ねる）に向け、撮影時点で顔の位置・大きさ・
傾きのズレを減らすため、撮影中にリアルタイムフィードバックを表示する機能を新設する範囲に
限り、iPadカルテ画面の凍結を解除した。

- 使用ライブラリ: `@mediapipe/tasks-vision`（顔検出モデル単体、`FaceDetector`）を新規導入。
  ブラウザ標準のShape Detection API(`FaceDetector`)はSafari(iPadOS含む)が未実装のため
  利用不可と判明し、この代替として選定した。
- WASMランタイム(約11MB、顔検出専用ではなく汎用ビジョンタスク共通のランタイム)とBlazeFace
  検出モデル(約230KB)は`public/mediapipe/`配下に自前ホスティングし、Google CDNへの
  実行時依存を避けた。
- `src/lib/photos/faceGuide.ts`: 大きさ・位置・傾きの判定を行う純粋関数(MediaPipe・DOMに
  非依存、ユニットテスト17件で検証済み)。閾値(`FACE_GUIDE_SIZE_MIN_RATIO`等)は全て
  実機チューニング前提のPhase1暫定値として定数化した(`CAPTURE_AUTO_CONFIRM_MS`と同じ方針)。
- `src/hooks/useFaceGuide.ts`: MediaPipeの実行バインディング。250ms間隔で推論し、モデルは
  アプリ内で1回だけロードして使い回す。
- `IpadPhotoCaptureModal.tsx`に統合: **正面**=大きさ・位置・傾きの3種フィードバック、
  **右斜め・左斜め**=大きさ・位置のみ(傾きの判定基準が未確定のため今回は見送り)、
  **額**=顔検出を行わず静的な枠ガイドのみ(額クローズアップは目・鼻・口が写らずモデルが
  顔として認識できない可能性が高いと判断したため)。既存のPhotoPanel(表示・比較用)には
  一切手を加えていない。
- 自動検証(Playwright、フェイクカメラデバイス使用、実際の顔検出精度の検証にはならない):
  額選択時に静的ガイドが表示されること、正面/右斜め/左斜め選択時にMediaPipeのWASM/モデル
  ロードを含めページエラーが発生しないこと、自前ホスティングしたアセットが全て200で
  取得できることを確認済み。検証用一時specはコミット前に削除。
  **実際の検出精度・iPad実機でのパフォーマンス(推論間隔250msの妥当性含む)は実機での
  試行錯誤が前提であり、まだ確認できていない。閾値調整・実機確認が完了するまでは
  この機能を「調整完了」とは判断しない。**

**この解除は上記の撮影ガイド機能新設に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・
LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き
凍結を継続する。

### v1.0.1 着手済み事項（撮影ガイドUI改善: 自動保存の視覚フィードバック追加＋ガイドメッセージ視認性向上のみ・2026-09-17ユーザー承認）

額タブの顔検出誤動作疑い（ユーザー報告: 「近づいてください」「離れてください」が交互に出て
判定が通らない）はREAD ONLY調査の結果、現在のコード・現在の本番デプロイ(コミット`b049fc3`)の
いずれでも額タブ選択時に顔検出ロジックが起動しないことを確認済み(`faceGuideModeFor()`が
`'forehead'`を`'none'`へマップし、`useFaceGuide.ts`は`mode==='none'`時にモデルロード・推論を
一切行わず、`IpadPhotoCaptureModal.tsx`のレンダリング分岐も`faceGuideMode==='none'`側では
動的判定文言に到達しない構造)。この件は原因未確定(PWAキャッシュ等の可能性はあるが断定せず)
のため今回のスコープには含めず、コード変更を行っていない。次回、最新の本番URLで実機再現
確認を行い、再現した場合はその時点で追加調査する。

今回実装したのは以下2点のUI改善のみ(ユーザー承認済み)。

- 自動保存の視覚フィードバック: `CaptureConfirmSession`の1.5秒自動確定タイマーが完了した
  瞬間(`reviewPhase`が`'reviewing'`→`'confirmed'`に変わる瞬間。ネットワーク応答=`justSaved`
  を待たないローカルな確定タイミング)に、約900msだけチェックマークの演出(画面フラッシュ＋
  アイコン)を表示するようにした。この演出は「サーバー保存が成功した」ことを示すものでは
  なく「この撮影がローカルで確定した」ことを示すものであり、`uploadError`が発生した場合は
  演出中でも即座に消す(保存失敗を「保存済み」と誤認させないため)。既存の
  「保存しました。続けて撮影・追加できます。」テキスト(サーバー保存成功=`justSaved`連動)は
  無変更のまま両立させた。
- ガイドメッセージの視認性向上: 額固定文言・動的判定文言(「もう少し近づいてください」等・
  「良い構図です」)の両方を、半透明の濃色背景付きピル型バー＋白文字太字・フォントサイズ
  拡大(12→14px)に統一し、屋内照明下でも判読しやすくした。状態(指示中/良好)は文字色だけに
  頼らず、色付きドット(ゴールド/緑)でも示す。ガイド枠(破線ボックス・円)の色ロジックは無変更。

変更ファイルは`src/components/customer/ipadKarte/IpadPhotoCaptureModal.tsx`の1ファイルのみ。
`usePhotoCapture.ts`・`captureConfirmFlow.ts`・`faceGuide.ts`・`useFaceGuide.ts`・API・DB
スキーマはいずれも無変更。`npm run typecheck`・`npm run build`・関連ユニットテスト
(`tests/lib/photos`116件・`tests/api/customer-photos-upload.test.ts`15件)は全てパス。
フルテストスイートで別途失敗する84件は、いずれもこのファイルを参照しない別モジュール
(voice-pipeline・DashboardAggregator・各種admin API等、既存の未コミット作業に起因)である
ことを確認済み。**実機(iPad)での視認性・演出タイミングの確認はまだ完了していない。実機確認が
完了するまでは、この機能を「調整完了」とは判断しない。**

**この解除は上記2点のUI改善に限る(額タブの顔検出疑いの調査・対応は含まない)。** 5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の
仕様については引き続き凍結を継続する。

### 額タブ「近づいて/離れて」疑いの再調査(2026-09-17・実機確認で解消)

上記の原因未確定のまま保留していた件について、READ ONLYでのコード再読解に加え、
Playwright(フェイクカメラデバイス、本番Supabase・実顧客「小宮山 仁美」)で実際に
額タブを選択した際のネットワークリクエストを実測した。額タブ選択時はMediaPipeの
WASM/モデルファイルへのリクエストが0件(正面タブでは3件発生し、監視方法自体が
機能していることも確認済み)、「近づいて/離れて」系テキストも0件であることを確認した。
その後スタッフによるiPad実機確認でも同症状が再現しないことが確認された
(2026-09-17ユーザー報告)。**コード変更は行っていない(修正すべき箇所が無かったため)。**
これにより本件は解消済みとして扱う。

### v1.0.1 着手済み事項（写真カルテ Phase 2: ゴースト機能・水平器/ジャイロガイド・ズーム倍率表示・全画面レイアウト化・2026-09-17ユーザー承認）

Phase 1(顔検出による位置・大きさ・傾きガイド)に続き、IMG_1448.JPGで確定したデザインに
合わせて撮影画面を全画面レイアウトへ変更し、ゴースト機能・水平器/ジャイロガイド・
ズーム倍率表示を追加する範囲に限り、iPadカルテ画面の凍結を解除した。既存の撮影・保存
ロジック(`usePhotoCapture.ts`・`captureConfirmFlow.ts`・`faceGuide.ts`・`useFaceGuide.ts`・
API・DB)には一切手を加えていない。

- **全画面レイアウト化**: `IpadPhotoCaptureModal.tsx`を中央カード(480px)からフルスクリーン
  へ変更。ヘッダー(ロゴ・部位タブ・意図ラベル・閉じる)、カメラ映像+右サイドバー(ゴースト
  操作、カメラ有効時のみ表示)、ガイドメッセージ帯(映像の外側の専用バーへ変更、視認性向上の
  ため濃色ピルから背景付き帯へ再設計)、シャッターの縦積み構成。既存のPhotoPanel(表示・
  比較用)には手を加えていない。
- **ゴースト機能**: 新規`src/hooks/useGhostOverlay.ts`。自動選択(前回優先→初回、
  `ghostSelection.ts`)自体はusePhotoCapture.tsの既存ロジック(`ghost`/`ghostUrl`)をそのまま
  使い、ON/OFFトグル・強さ(連続0-100%、既定30%・デザイン確定値)・前回撮影日時の表示・
  日付を手動で選び直す機能(縦リスト、既存の`GET /api/customers/[id]/photos`をそのまま
  利用・新規API無し)を上乗せした。ON/OFF・強さはlocalStorageに永続化(usePhotoCapture.tsの
  既存ghostOpacityLevel復元と同じパターン)。位置合わせ(移動・回転)は実装していない
  (「カメラを動かす」運用で対応、ユーザー確定済み仕様)。
- **水平器/ジャイロガイド**: 新規`src/lib/photos/tiltGuide.ts`(純粋関数、ユニットテスト
  8件)・`src/hooks/useDeviceTilt.ts`(DeviceOrientationEventバインディング)。
  **設計上の割り切り**: beta/gammaの絶対値は機種・画面の向きで意味が変わり実機なしでは
  校正できないため、絶対値ではなく「ガイド開始時点(基準姿勢)からの相対的なズレ」のみを
  見る方式にした(閾値`TILT_TOLERANCE_DEG`はPhase1の`FACE_GUIDE_*`と同じ位置づけの暫定値)。
  iOS(iPadOS含む)Safari 13+は`DeviceOrientationEvent.requestPermission()`が
  **ユーザーの直接タップ操作の中で同期的に呼ばないと拒否される**仕様のため、
  useEffect起点では自動リクエストせず、カメラ映像上に「水平ガイドを有効にする」ボタンを
  一時的に表示しユーザーのタップを介してリクエストする。額タブ(顔検出なし)でも
  ジャイロガイドは有効にした(仕様C: 額タブは静的枠ガイド＋ジャイロのみで運用)。
  ターゲットクロスの位置は実機での校正前提のため画面中央付近に固定表示とし、色(緑/白)
  のみで整列状態を伝える簡易実装(顔検出結果への動的追従は今回のPhaseでは行っていない)。
- **ズーム倍率表示**: 「1x」バッジ(ロックアイコン付き)を表示。**実際の倍率ロックではなく
  表示のみ**(技術確認済み: `getUserMedia`の`zoom`制約はiPadOS Safariでは機能しない
  Chrome/Android限定の非標準拡張のため)。アプリの`viewport`設定(`app/layout.tsx`、
  `userScalable:false`・`maximumScale:1`)により、ページ全体のピンチズームは元々
  無効化済みであることも確認した。
- ガイドメッセージの優先順位を拡張: 顔ガイド(近い/遠い/位置/傾き) > 端末の傾き(ジャイロ)
  > ゴーストの位置合わせ案内(「前回の写真に合わせて位置を揃えてください」) >
  「良い構図です」。額タブは顔ガイドが無いため、ジャイロ→ゴースト→固定文言の順になる。
- 自動保存の視覚フィードバック(チェックマーク演出)・ガイドメッセージの視認性向上
  (濃色帯+色分けドット)はPhase 1時点の実装を維持しつつ、新しい全画面レイアウトへ
  そのまま統合した。
- 変更・新規ファイル: `src/components/customer/ipadKarte/IpadPhotoCaptureModal.tsx`
  (全面書き換え)、新規`src/hooks/useGhostOverlay.ts`・`src/hooks/useDeviceTilt.ts`・
  `src/lib/photos/tiltGuide.ts`・`tests/lib/photos/tiltGuide.test.ts`。
  `usePhotoCapture.ts`・`captureConfirmFlow.ts`・`faceGuide.ts`・`useFaceGuide.ts`・
  `ghostSelection.ts`・API・DBスキーマはいずれも無変更。
- 検証: `npm run typecheck`・`npm run build`は成功、`tests/lib/photos`(tiltGuide 8件を
  含め124件)は全てパス。Playwright(フェイクカメラデバイス、本番Supabase・実顧客
  「小宮山 仁美」)で、額タブの固定文言・1xバッジ表示、正面タブでのゴーストサイドバー
  (トグル・強さ)表示、ゴーストON/OFF切替・強さ変更・日付リスト展開のいずれもpage error
  無く動作することを確認済み(検証用一時specはコミット前に削除)。
  **実際のiPad実機でのゴースト表示・強さ調整・日付切替の操作感、ジャイロの精度・
  権限リクエストの実際の挙動、ズームバッジの見え方は、実機での試行錯誤が前提であり
  まだ確認できていない。実機確認が完了するまでは、この機能を「調整完了」とは判断しない。**

**この解除は上記の全画面レイアウト化・ゴースト機能・水平器/ジャイロガイド・ズーム倍率表示の
新設に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の
顧客タブ/iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（ゴースト初期倍率の自動調整・サイズスライダー追加のみ・2026-09-18ユーザー承認）

小宮山様での実機フィードバック(「ゴーストの写真が大きすぎる」)により、上記Phase 2の
ゴースト機能が「位置合わせ(拡大縮小)は実装せず、カメラを動かす運用に委ねる」設計のままでは
実用上不十分と判明したため、以下の範囲に限りiPadカルテ画面の凍結を解除した。既存の撮影・
保存ロジック(usePhotoCapture.ts・captureConfirmFlow.ts・faceGuide.ts・useFaceGuide.tsの
判定ロジック本体・ghostSelection.ts・API・DB)には一切手を加えていない。

- 新規`src/lib/photos/ghostAutoScale.ts`(純粋関数、ユニットテスト8件): ライブ映像の顔検出
  結果とゴースト静止画の顔検出結果それぞれの「顔の高さ/フレーム高さ」比を比較し、ゴーストに
  掛けるべき初期倍率(50%〜200%にクランプ)を算出する。
- 新規`src/hooks/useGhostImageFaceDetection.ts`: ゴースト静止画に対して一度だけ顔検出を
  行うための別インスタンス(runningMode:'IMAGE')。ライブ映像用の既存検出ループ
  (useFaceGuide.ts、runningMode:'VIDEO')とは別インスタンスにして干渉を避けた。CORS等の
  理由で検出に失敗しても例外を投げずnullを返すだけ(自動調整されないだけで機能全体は
  止まらない)。
- `useFaceGuide.ts`に`sizeSample`(顔ボックス高さ・フレーム高さの生の値)を追加。既存の
  `state`(ok/too_close等の分類結果、faceGuide.tsの判定ロジック本体)には手を加えていない。
- `useGhostOverlay.ts`に`scalePercent`/`setScalePercent`(50-200%、既定100%)を追加。
  「強さ(不透明度)」とは異なり、ちょうど良い倍率は部位・お客様・撮影距離ごとに変わるため
  localStorageへは永続化せず、表示するゴースト写真が変わるたびに既定値へリセットする設計
  にした。
- `IpadPhotoCaptureModal.tsx`: ゴーストサイドバーに「サイズ」スライダーを追加(「強さ」の
  下)。表示中のゴースト写真が切り替わった直後、両者の顔検出結果が揃った時点で一度だけ
  自動倍率を適用し(ライブ映像に継続追従はしない設計。継続追従すると常に大きさが一致して
  見えてしまい、カメラを動かして合わせるためのガイドとして機能しなくなるため)、以降は
  この手動スライダーで調整する。ゴースト<img>要素はpointerEvents:noneのままで、撮影ガイド
  表示・シャッター操作とは独立している。保存される写真は従来通り`<video>`フレームのみから
  生成され(captureFrame.ts、無変更)、ゴーストへの参照を一切持たないため「保存画像に
  ゴーストが焼き込まれない」構造的保証は変わらない。
- 検証: `npm run typecheck`・`npm run build`は成功、`tests/lib/photos`(新規ghostAutoScale
  8件を含め148件)は全てパス。**実機(iPad)での自動倍率の精度・体感、ゴースト静止画に対する
  顔検出の実際の成功率(署名URLのCORS挙動含む)は実機での試行錯誤が前提であり、まだ確認
  できていない。実機確認が完了するまでは、この機能を「調整完了」とは判断しない。**

**この解除は上記のゴースト初期倍率自動調整・サイズスライダー追加に限る。** 5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ
画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（ゴーストの自動位置・サイズ合わせ(継続追従)への置き換え・2026-09-18ユーザー承認）

上記「ゴースト初期倍率の自動調整」を実機で確認した結果、「写真切替時に一度だけ倍率を
計算する方式」では計算した瞬間からカメラが動くとズレてしまい、調整精度・使い勝手が
不十分と判明した。ユーザーから「既存のMediaPipe顔検出を活用して、ゴーストの位置と
サイズを自動調整できるようにする」「調査報告だけで終了せず実装する」との明示指示を
受け、以下の範囲に限りiPadカルテ画面の凍結を再度解除した。既存の撮影・保存ロジック
(usePhotoCapture.ts・captureConfirmFlow.ts・faceGuide.tsの判定ロジック本体・
useFaceGuide.tsの検出ループ本体・ghostSelection.ts・API・DB)には一切手を加えていない。

- **設計方針の転換**: 従来「位置合わせ(移動)は実装せず、カメラを動かす運用に委ねる」
  としていた仕様(本ファイル上部の写真カルテ Phase 2の記載)を撤回し、位置(平行移動)・
  大きさ(倍率)の両方をライブ映像の顔検出結果に継続的に追従させる方式へ変更した。
  以前は「継続追従させると常に一致して見えてガイドとして機能しなくなる」ことを懸念して
  意図的に一度きりの適用にしていたが、今回のユーザー指示は「カメラを動かして合わせる」
  運用自体をこの自動追従に置き換える方針への転換であるため、その懸念は前提ごと解消される
  (合わなくなるのは顔検出に失敗した場合のみで、その場合は手動フォールバックに委ねる)。
- `src/lib/photos/ghostAutoScale.ts`(前身、写真切替時に一度だけ倍率計算)を削除し、
  `src/lib/photos/ghostAlignment.ts`(純粋関数、ユニットテスト7件)に置き換えた。
  ライブ映像・ゴースト静止画それぞれの「顔ボックス+ネイティブ解像度」と、実際の表示枠
  (box)のサイズから、object-fit:coverによる縦横クロップを考慮して顔の画面上位置・
  大きさを計算し、両者を一致させるためのCSS transform(translate+scale、および
  scaleの基準点となるtransform-origin)を算出する。
- `src/hooks/useGhostImageFaceDetection.ts`: 返り値を高さのみ(`faceBoxHeight`/
  `frameHeight`)から顔ボックス全体+画像解像度(`face`/`frame`、x/y座標を含む)に拡張した。
  ゴースト静止画は動かないため引き続き1回だけ検出すれば足りる。
- `src/hooks/useFaceGuide.ts`: 返り値`sizeSample`(高さのみ)を`rawDetection`(顔ボックス
  全体+video解像度)に拡張・改名した。既存の250ms間隔の検出ループ・`state`(ok/too_close
  等の分類結果)自体には手を加えていない。
- `IpadPhotoCaptureModal.tsx`: 映像+ゴーストを包む枠のDOM要素にコールバックrefで
  `ResizeObserver`を張り、実際の表示サイズ(box)を取得できるようにした
  (reviewPhase切替でこのdivがアンマウント/再マウントされてもコールバックrefにより
  張り直される)。`ghostAlignment`(ライブ検出結果が更新される都度、既存の250ms間隔で
  再計算される)が算出できた場合はゴースト`<img>`にtranslate+scaleを適用して自動追従させ、
  算出できない場合(顔未検出、額タブ等)は従来通り等倍・中央表示+手動「サイズ」スライダー
  (この機能で追加済み)にフォールバックする。手動スライダーは自動合わせの結果に対する
  追加の微調整倍率として残した(既定100%=無補正)。ガイドメッセージの「前回の写真に
  合わせて位置を揃えてください」は、自動追従が効いている間は表示せず、顔検出できず
  自動合わせが効かない場合のみのフォールバック表示にした。サイドバーにも自動/手動の
  状態を示す小さな表示を追加した。
- 検証: `npm run typecheck`・`npm run build`は成功、`tests/lib/photos`(新規
  ghostAlignment 7件を含め147件)は全てパス。**実機(iPad)でのカメラの動きに対する
  追従の滑らかさ・精度、ResizeObserverの実際の挙動(特にreviewPhase切替時の張り直し)は
  実機での試行錯誤が前提であり、まだ確認できていない。実機確認が完了するまでは、この
  機能を「調整完了」とは判断しない。**

**この解除は上記のゴースト自動位置・サイズ合わせへの置き換えに限る。** 5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ
画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（ゴースト自動位置・サイズ合わせの再設計: 既存の顔検出ガイド基準への置き換え・2026-09-18ユーザー承認）

直前の「ライブ映像への継続追従(位置・サイズを250ms間隔で常に再計算)」方式を実機で
確認した結果、検出結果のフレームごとのブレがそのままゴーストの微振動として見えてしまい
(ユーザー報告「ゴーストが下でちょこちょこ動いてるだけで無理そう」)、使い物にならないと
判明した。スムージング(指数移動平均+検出途切れの保持)による改善を1サイクル試みたが、
ユーザーから「継続追従を複雑にするより、既存の顔検出ガイド(丸い輪)を基準にする方式を
検討したい」との明示指示を受け、方式そのものを再設計した。以下の範囲に限り
iPadカルテ画面の凍結を再度解除した。既存の撮影・保存ロジック(usePhotoCapture.ts・
captureConfirmFlow.ts・faceGuide.ts・useFaceGuide.tsの検出ループ本体・ghostSelection.ts・
API・DB)には一切手を加えていない。

- **事前調査(ユーザー指示どおり、まず既存の丸いガイドが固定か検出結果依存かを確認)**:
  `IpadPhotoCaptureModal.tsx`内の顔検出ガイド(丸い破線の輪、正面/右斜め/左斜めで表示)は
  `left:28%/right:28%/top:14%/bottom:18%`という**固定パーセンテージ**で描画されており、
  検出結果に応じて動かないことを確認した(枠の色だけがガイド状態に応じて変わる)。
  この事実が今回の再設計の前提になっている。
- **新方式**: ゴースト(前回写真)の顔をこの固定の輪と同じ「ちょうど良い大きさ・位置」の
  基準(既存のfaceGuide.tsが判定に使う`FACE_GUIDE_SIZE_MIN_RATIO`〜`MAX_RATIO`の中央値・
  画面中央0.5,0.5)に、写真が切り替わった時に一度だけ合わせる。今回撮る側(ライブ映像)は
  従来通り既存のuseFaceGuide.ts/faceGuide.tsのフィードバックでこの同じ基準に収まるよう
  案内される。両者が同じ固定基準に揃うことで、結果的にゴーストとライブが一致する
  (ライブ検出への継続追従は不要と判断し廃止)。
- `src/lib/photos/ghostAlignment.ts`を全面書き換え: `computeGhostAlignment(box,live,ghost)`
  (ライブ検出との比較、ジッターの原因)を削除し、`computeGhostRingAlignment(box,ghost)`
  (ゴースト自身の検出結果のみを固定ターゲットに合わせる、ライブ側には非依存)に置き換えた。
  スムージング用の定数・`lerpFaceBox`も不要になったため削除した。ユニットテスト
  (`tests/lib/photos/ghostAlignment.test.ts`)も新方式に合わせて全面書き換え(7件)。
- `src/hooks/useSmoothedFaceDetection.ts`(前サイクルで追加したスムージングフック)を
  削除。ライブ検出結果を使わなくなったため不要になった。
- `src/hooks/useFaceGuide.ts`の`rawDetection`(前サイクルで追加した生の検出値の公開)を
  削除し、Phase 1時点の形(`state`/`modelLoading`/`modelError`のみ)に戻した。判定ロジック
  本体(`state`の算出、`faceGuideMessage`)には最初から一切手を加えていない。
- `src/hooks/useGhostImageFaceDetection.ts`: 変更なし(ゴースト静止画への一度きりの
  顔検出はそのまま再利用、コメントのみ新方式に合わせて更新)。
- `IpadPhotoCaptureModal.tsx`: ゴーストのtransform算出元を`computeGhostRingAlignment`に
  切り替え。表示枠のResizeObserverは(輪の位置計算に box サイズが必要なため)引き続き
  使用。手動「サイズ(微調整)」スライダーは既定100%のまま自動合わせに対する追加倍率
  として維持。サイドバーの状態表示文言を「ガイドの輪に合わせて自動調整しています」に
  更新した。
- 検証: `npm run typecheck`・`npm run build`は成功、`tests/lib/photos`(新方式の
  ghostAlignment 7件を含め147件)は全てパス。**実機(iPad)での見た目・精度はまだ
  確認できていない。実機確認が完了するまでは、この機能を「調整完了」とは判断しない。**

**この解除は上記のゴースト自動位置・サイズ合わせの再設計に限る。** 5タブ構成・TL-5構成・
AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様
については引き続き凍結を継続する。

### v1.0.1 着手済み事項（ゴースト表示不具合調査・四角い切り抜きの解消・2026-09-18ユーザー承認）

実機確認で「①ゴーストが中央で四角く切り取られ境界が目立つ」「②ゴーストが上下逆さまに
表示されライブと向きが一致しない」の2件が報告された。以下の範囲に限りiPadカルテ画面の
凍結を再度解除した。既存のMediaPipe判定ロジック・撮影ガイド・シャッター操作・DB/Storage
には一切手を加えていない。

- **②上下逆さま問題の調査結果**: 以下の全経路をコード監査したが、回転・上下反転を
  発生させるコードは見つからなかった。
  - ゴースト`<img>`のCSS(`IpadPhotoCaptureModal.tsx`): `transform`はtranslate/scaleのみで
    rotate/scaleYは一切使用していない。`ghostAlignment.ts`の`scale`は数式上常に正の値
    (0除算・負値のガードあり、クランプ範囲もGHOST_ALIGNMENT_SCALE_MIN(0.4)〜MAX(2.5)の
    正の範囲)であることをコードで確認済み。
  - ライブ撮影経路(`captureFrame.ts`): `<video>`要素からcanvasへの描画のみで、video
    フレームにはEXIFという概念自体が無く(OS/ブラウザがgetUserMediaに渡す時点で
    デバイスの向きに応じた補正が完了している)、ライブプレビューに見えている通りの
    ピクセルがそのまま保存される(WYSIWYG)。
  - ファイル選択経路(`fileToWebpBlob.ts`): `<img>`要素でファイルを読み込みcanvasへ
    描画している。iOS Safari(このアプリの対象環境)は`<img>`のレンダリングおよび
    `canvas.drawImage()`のソースとしての利用の両方でEXIF Orientationタグを解釈する
    (JPEG・HEICとも、iOS 13以降で確立された標準動作)ため、この経路でも回転情報の
    ロスは起きないはずと判断した。
  - サーバー側(`POST /api/customers/[id]/photos`等): sharp等の画像処理ライブラリは
    一切使用しておらず、クライアントが送ったBlobをそのままStorageに保存するのみ
    (grep調査で確認済み)。
  - **結論**: コード上に回転・反転を引き起こす箇所が見つからなかったため、根拠のない
    rotate/scaleYの追加(帳尻合わせ)は行っていない(ユーザー指示どおり)。最も疑わしい
    仮説は、額の接写のような不自然な体勢で撮影する際にiPadが物理的に180度回転した状態で
    保持されていた(ライブプレビュー・保存ピクセルの両方がその向きのまま記録される、
    アプリの不具合ではなく撮影時の実際の向き)という説明だが、未検証。
  - **未完了の調査**: 実際に保存されている写真ファイル自体の向きを確認するため、本番
    Supabaseの`brain_customer_photos`テーブルをREAD ONLYで参照しようとしたが、実行環境の
    権限設定(自動判定)により本番DBへの読み取りクエリがブロックされ、確認できなかった。
    この写真が「ゴースト表示でだけ」上下逆さまなのか、`前回/今回`比較パネル等どこで見ても
    同じく上下逆さまなのか(=ファイル自体の向きの問題と確定できる)を切り分けるため、
    実機で同じ写真を写真管理モーダル(🗑写真を削除、サムネイル一覧)や前回/今回比較で
    見た時にも上下逆さまかどうかの確認をお願いしたい。逆さまなら本件はゴースト表示固有の
    不具合ではなく撮影時の保存データそのものの向きの問題であり、対応(再撮影を促す、
    もしくは別途向き補正機能を設計する)は別途相談が必要(DB/Storage変更を伴うため今回の
    スコープ外)。
- **①四角い切り抜き問題の修正**: `src/lib/photos/ghostAlignment.ts`の`GhostAlignmentResult`
  に`maskRadiusX`/`maskRadiusY`(顔ボックスに`GHOST_MASK_FACE_MARGIN`(暫定値1.7倍)の
  余白を掛けた楕円マスクの半径)を追加した。「顔領域だけを自然に重ねる」か「画像全体を
  自然な縁で重ねる」かを比較し、前者を採用した(既存の丸い顔検出ガイドと同じ楕円の
  見た目で視覚的に揃えられる、前回写真の髪型・服・背景まで写り込んで比較の邪魔になる
  ことを避けられるため、このアプリの用途(顔の位置合わせ)により適していると判断)。
  `IpadPhotoCaptureModal.tsx`のゴースト`<img>`に`mask-image`/`-webkit-mask-image`
  (radial-gradientの楕円、原点はghostAlignment.originXと同じ)を追加した。自動合わせが
  効かない(顔検出できない)場合はマスクを適用せず、従来通り画像全体をそのまま重ねる
  フォールバックを維持している(顔検出失敗時も撮影を継続できることを確認済み)。
- 変更ファイル: `src/lib/photos/ghostAlignment.ts`(マスク半径の算出を追加)、
  `src/components/customer/ipadKarte/IpadPhotoCaptureModal.tsx`(mask-image適用のみ、
  MediaPipe判定ロジック・撮影ガイド・シャッター操作には手を加えていない)、
  `tests/lib/photos/ghostAlignment.test.ts`(マスク半径のアサーションを追加)。
- 検証: `npm run typecheck`・`npm run build`は成功、`tests/lib/photos`(147件)は全て
  パス。保存写真がゴーストを含まないことは、captureFrame.tsが`<video>`フレームのみを
  描画し、ゴースト`<img>`への参照を一切持たない構造から従来通り保証される(無変更)。
  **iPad実機未確認**: ①マスクの見た目(自然さ、余白倍率1.7の妥当性)、②上記の
  「他のどの画面で見ても上下逆さまか」の切り分け結果、のいずれも実機確認が必要。
  実機確認が完了するまでは、この対応を「調整完了」とは判断しない。

**この解除は上記の四角い切り抜きの解消(マスク追加)に限る。上下逆さま問題はコード上の
原因が特定できず、根拠のない変更を避けるため今回は対応していない(調査結果は上記の通り、
ユーザーへの追加確認依頼が必要)。** 5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・
admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き凍結を継続する。

### ゴースト機能の一時停止(2026-09-18ユーザー指示)

実機確認を重ねた結果、ゴースト自動位置・サイズ合わせは依然ズレが解消しないと判断され、
ユーザー指示により**この機能の追加改修を一旦停止**した。既存のコード
(`ghostAlignment.ts`・`useGhostImageFaceDetection.ts`・`useGhostOverlay.ts`・
`IpadPhotoCaptureModal.tsx`のゴースト関連部分)はそのまま残しており、削除・無効化は
していない(機能自体はON/OFFトグルで従来通り使用可能)。次回以降、ゴースト機能に着手する
際は、直近3回の実機フィードバック(ジッター→固定ガイド輪基準への再設計→四角い切り抜きの
マスク化)の経緯と、それでも解消しなかったズレの詳細をユーザーに確認してから着手すること。

### v1.0.1 着手済み事項（ゴースト機能: 選定ロジックの根本原因調査・修正のみ・2026-09-18ユーザー承認）

一時停止中のゴースト機能について、実機で「今日撮った写真がゴーストとして今日また表示
される」「額タブでゴーストに斜めの帯状表示が出る」という新規報告を受け、READ ONLY調査を
行った結果、上記までの5回の改修が一貫して`ghostAlignment.ts`(位置・拡大率の計算)側の
対症療法だったのに対し、根本原因は一度も触れられていなかった`ghostSelection.ts`(ゴースト
写真の選定ロジック、2026-09-03作成以降無変更)側にあると判明した。ユーザー承認を得て、
以下3点の修正のみに限りiPadカルテ画面の凍結を再度解除した。既存の撮影・保存ロジック
(usePhotoCapture.tsの撮影確定フロー本体・captureConfirmFlow.ts・faceGuide.ts・
useFaceGuide.tsの検出ループ本体・API・DB)には一切手を加えていない。

- **①選定ロジックを「撮影機会(occasion)」単位に統一**: iPadスタッフカルテは撮影時点で
  `brain_visits`(来店記録)が未作成(CSV取込または接客ログ保存でしか作られず、いずれも
  施術後に発生するため)なことが常態で、その間に保存される写真の`visit_id`は
  `src/lib/photos/linkPhotosToVisit.ts`が前提とする通り全てnullになる。`ghostSelection.ts`
  の旧実装(`visit_id ?? 'none'`という単一キーでの重複排除+`currentVisitId`がnullの間は
  除外自体が効かない仕様)は、この状態下で「数分前に撮った同日の写真」を「前回」として
  誤選択していた。同じ問題は`comparisonSelection.ts`(前回|今回比較パネル用)で
  2026-09-06に一度発見・修正済み(`visit_id`があれば`visit:${id}`、無ければ撮影日
  `date:${YYYY-MM-DD}`をキーにする「撮影機会」単位のグルーピング)だったが、この修正は
  3日前に作られていた`ghostSelection.ts`には移植されていなかった。今回、同じ
  occasion単位のロジックを`ghostSelection.ts`にも適用した(`excludingCurrentOccasion`・
  `representativePerOccasion`)。
- **②「今日を除外する」ロジックの一本化**: `ghostSelection.ts`(重複排除+除外の2実装)・
  `useGhostOverlay.ts`(日付手動選択候補リストの除外、`visitId`nullの扱いが前者と食い違って
  いた)の計3箇所に分散していた実装を、`ghostSelection.ts`の`excludingCurrentOccasion()`
  (exportに変更)への一本化にまとめた。
- **③額タブのゴースト静止画に対する顔検出をスキップ**: `faceGuideModeFor()`は既に
  「額クローズアップは目・鼻・口が写らずモデルが顔として認識できない可能性が高い」との
  判断でライブ映像側の顔検出ガイドを額タブのみ無効化していたが、この判断は
  `useGhostImageFaceDetection`(ゴースト静止画への一度きりの顔検出)には引き継がれておらず、
  額タブでも無条件に検出を実行していた。誤検出結果が`computeGhostRingAlignment`の
  スケール・マスク計算にそのまま使われることが「斜めの帯状表示」の原因と推定されたため、
  `IpadPhotoCaptureModal.tsx`側で`faceGuideMode !== 'none'`のガードを追加し、額タブでは
  常に検出自体を呼ばない(既存の「顔検出できない場合の等倍・中央表示+手動スライダー」
  フォールバックへ合流させる)ようにした。
- 上下逆さま問題(④)は今回もスコープ外(ユーザー指示により棚上げ継続)。
- 検証: `npm run typecheck`・`npm run build`は成功、`tests/lib/photos`(新規の同日自己
  参照除外テスト2件を含む)は全てパス。Playwright(`--use-fake-device-for-media-stream`、
  本番Supabase・実顧客「小宮山 仁美」、検証用一時spec・コミット前に削除済み)による実機
  確認: 同一body_part(正面)で2枚連続撮影した際、2枚目の直前に表示される自動選択ゴーストが
  1枚目自身(当日・`visit_id`無し)を指さず、既存の実際の前回データ(2026-09-13)を正しく
  指すことを確認した。額タブについても、事前に登録した古い日付のダミー写真がゴーストとして
  表示される状態で「○ 顔未検出(手動調整)」に固定され「● 自動調整中」(=検出結果を使った
  自動合わせ)には一切ならないことを確認した。作成したテストデータ(4件)は検証後に削除済み。

**この解除は上記①②③の修正に限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・
admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（写真撮影画質改善 Phase 3-A の反映のみ・2026-09-18ユーザー承認）

小宮山様の実機撮影(人形の練習ヘッドを使用)で写真がピンボケ気味だったことの切り分けとして、
以前から用意されていた未コミットの「写真撮影画質改善 Phase 3-A」のみを対象に本番反映した。
この対応はゴースト機能とは無関係(ゴースト機能は上記の通り一時停止中)。

- `src/lib/photos/captureFrame.ts`: `MAX_CAPTURE_LONG_EDGE_PX`を1920→3072pxへ、
  `encodeCanvasWithFallback`の既定エンコード品質を0.8→0.9へ引き上げ。
- `src/hooks/usePhotoCapture.ts`: `CAMERA_CONSTRAINTS`(新規export、
  `facingMode:{ideal:'environment'}`・`width:{ideal:3072}`・`height:{ideal:2304}`)を
  追加し、`startCamera()`のgetUserMedia呼び出しをこれに差し替えた。**この作業対象の
  ファイルには「一時診断(手ブレ/モーションブラー調査用)」のconsole.logコードが別途
  未コミットで混在していたが、これはPhase 3-Aとは無関係の別作業のため、gitのINDEXに
  直接blobを組み立てて(`git hash-object`+`git update-index --cacheinfo`)Phase 3-A分の
  変更だけをステージし、作業ツリー上の診断コードには一切手を加えず温存した。**
- `tests/lib/photos/captureFrame.test.ts`: 上記の値変更に合わせてテストを更新
  (1920→3072、0.8→0.9)。
- 検証: コミット対象のみを反映した状態を`git worktree`で分離したクリーンな
  チェックアウトを作り、そこで`npm run typecheck`・`tests/lib/photos`(147件)・
  `npm run build`を実行し、いずれも成功することを確認した(ローカルの未コミット・
  未追跡ファイルが検証に紛れ込まないようにするため)。
- 除外した無関係な未コミット変更: `src/lib/photos/constants.ts`・
  `tests/api/customer-photos-upload.test.ts`(HEIC対応・アップロード上限15MB化、
  別名称「写真カルテ Phase A(原本保存)対応」で本件とは別機能)、
  `app/api/cron/dashboard-aggregator/route.ts`・`src/components/customer/
  VoiceMemoSection.tsx`・`src/lib/contraindication.ts`・`package.json`
  (`supabase` CLI依存追加)等は一切含めていない。
- **iPad実機未確認**: 3072px・品質0.9化によって実際にピンボケが解消するか、
  それとも解像度ではなくピント/手ブレ自体が原因で改善しないかは、本番反映後に
  同一iPad・同一撮影条件で撮り直して比較する必要がある(ユーザーが実施予定)。
  改善しなかった場合はカメラのフォーカス制御(オートフォーカスのロック等)を
  次の検討対象とする方針(ユーザー確定)。

**この解除は上記の写真撮影画質改善 Phase 3-A の反映のみに限る。** 5タブ構成・TL-5構成・
AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様
については引き続き凍結を継続する。

### v1.0.1 着手済み事項（写真カルテUI改善 Part 4: 今回のホームケア・次回の目安の一時非表示・2026-09-18ユーザー承認）

スタッフからの操作フィードバックを反映する「写真カルテUI改善」(Part 1〜4)のうち、
影響範囲が最も小さいPart 4(表示の一時停止)から着手した。事前にREAD ONLY調査を行い、
表示箇所が`CustomerModeView.tsx`(お客様モード)・`IpadStaffKarteView.tsx`(スタッフ側
iPadカルテ)の2箇所であることを確認した上で実装した。以下の範囲に限りiPadカルテ画面・
お客様モード画面の凍結を再度解除した。

- 両ファイルの先頭に`SHOW_HOMECARE`/`SHOW_NEXT_VISIT_ESTIMATE`(いずれも`false`)を追加し、
  「今回のホームケア」カード・「次回の目安」表示のJSXをこのフラグでガードした。
  データ取得(`data.homecareItems`・`useNextVisit`フック)・API・DBには一切手を加えて
  いない(フラグは表示のみを止める)。再表示する場合は両ファイルの値を`true`に戻すだけで
  よい(2箇所は必ず同時に切り替えること)。
- `CustomerModeView.tsx`には既存の`nextVisit.hiddenFromCustomer`(顧客ごとにスタッフが
  個別設定する別機能、`IpadStaffKarteView.tsx`の`NextVisitCard`内にトグルUIがある)が
  あるが、これとは無関係の一律停止であり、今回は両条件のANDで判定する
  (`SHOW_NEXT_VISIT_ESTIMATE && !nextVisit.hiddenFromCustomer`)。既存の個別トグルの
  値・動作自体は変更していない。
- `IpadStaffKarteView.tsx`側は`NextVisitCard`コンポーネントごと条件レンダリングで止めて
  いるため、非表示中は内部の`useNextVisit`呼び出しも実行されず、無駄なAPI呼び出しも
  発生しない(データ削除ではなく、単にマウントされないだけ)。
- サブスク情報の表示・ロジックには触れていない(スコープ外、ユーザー指示どおり)。
- 検証: `npm run typecheck`・`npm run build`は成功。該当UI文言を参照するテストは
  存在しないことを事前に確認済み(`tests/`配下に「今回のホームケア」「次回の目安」等の
  文字列を参照するテストなし)。**実機(iPad)での見た目確認はまだ完了していない。**

**この解除は上記の表示一時停止(Part 4)のみに限る。** 続くPart 1(撮影画面の操作配置)・
Part 2(顔位置合わせガイド)・Part 3(お客様モードの写真追加)は別途着手する。5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域については引き続き凍結を継続する。

### v1.0.1 着手済み事項（写真カルテUI改善 Part 1: 撮影画面の操作配置・2026-09-18ユーザー承認）

Part 4に続き、Part 1(撮影画面の操作配置)に着手した。事前のREAD ONLY調査で「シャッターを
画面右側中央へ」「ゴーストUIを画面下部へ」の2点が実は前提・帰結の関係にある(ゴースト
サイドバーが右側260pxを占有していたため、先に退かさないとシャッターと場所を取り合う)と
確認済みだったため、両方をセットで実装した。`IpadPhotoCaptureModal.tsx`のみの変更で、
Part 2(顔位置合わせガイドの形状)・Part 3(お客様モードの写真追加)は今回含めていない。

- **ゴースト調整UIを画面下部へ**: 映像の右側にあった固定幅260pxのサイドバー(縦積み)を
  廃止し、映像の下・ガイドメッセージ帯のさらに下に、横一列の帯として配置し直した。
  「強さ」「サイズ(微調整)」スライダー・前回日付選択の中身・onChangeハンドラは無変更
  (`ghost.opacityPercent`/`ghost.scalePercent`等の値の受け渡しに一切手を加えていない)。
  日付候補リストは画面下端で切れないよう、下向きではなく上向きに開くよう位置だけ変更した。
- **シャッターを画面右側中央へ**: 従来は映像の下で横方向中央に配置していたボタンを、
  `position:absolute`で最外殻コンテナ(`position:fixed inset:0`)を基準とした右端・
  縦方向中央のオーバーレイに変更した(`right: max(20px, env(safe-area-inset-right))`・
  `top:50%`+`translateY(-50%)`)。表示条件(`cameraStatus==='ready'`)・無効化条件
  (`!bodyPartSelected`)・`onClick`(`capture.shutter()`)は無変更。フローから外れたことで
  映像領域の縦幅がわずかに広がる副次効果がある。
- ゴーストの自動位置・サイズ合わせ(`ghostAlignment.ts`)・マスク・回転・拡大縮小ロジックは
  一切変更していない(見た目の配置のみの変更、確認済み)。正面・左45°・右45°のガイド
  切替(`faceGuideModeFor`)にも触れていない。
- 検証: `npm run typecheck`・`npm run build`は成功、`tests/lib/photos`+
  `tests/hooks/usePhotoCapture.test.ts`(151件)は全てパス(コンポーネント自体はこの
  コードベースの慣習上ユニットテスト対象外)。**iPad実機での片手操作の実際のしやすさ・
  縦横比変更時の見え方はまだ確認できていない。実機確認が完了するまでは、この対応を
  「調整完了」とは判断しない。**

**この解除は上記のPart 1(撮影画面の操作配置)のみに限る。** 続くPart 2(顔位置合わせ
ガイド)・Part 3(お客様モードの写真追加)は別途着手する。5タブ構成・TL-5構成・AI提案の
会話トーン・LINE領域・admin領域については引き続き凍結を継続する。

### v1.0.1 着手済み事項（写真カルテUI改善 Part 2: 顔位置合わせガイドの形状変更・2026-09-18ユーザー承認）

Part 1に続き、Part 2(顔位置合わせガイド)に着手した。「丸い顔ガイドより十字のグリップ線の
方が使いやすい」というスタッフ意見を反映し、`IpadPhotoCaptureModal.tsx`のみを対象に
最小限の変更を行った。

- 正面/左45°/右45°(`faceGuideMode !== 'none'`)で表示していた丸い破線の輪を、
  三分割グリッド線(縦2本・横2本、常時薄い白の固定色)+中央の十字線に置き換えた。
  中央の十字線の色は従来の輪と同じロジック(`faceGuideMessage`があれば白=未調整、
  無ければゴールド=調整OK)を踏襲している。
- 額タブ(`faceGuideMode === 'none'`)の角丸長方形ガイドは今回のスコープ外のため無変更。
- 既存の水平器/ジャイロ用クロス(lucideの`Crosshair`アイコン、傾き確認専用、位置は
  50%/40%)は別要素のまま維持し、新しい顔ガイド十字(50%/50%)とは役割・見た目を
  分けている。
- `faceGuideModeFor()`(正面/左45°/右45°の切替判定ロジック)・`ghostAlignment.ts`
  (ゴーストの自動位置合わせ・マスク・回転・拡大縮小)には一切手を加えていない
  (このガイドのCSS座標を読んでいるコードがどこにも無いことをPart 1調査時に確認済み)。
- 検証: `npm run typecheck`・`npm run build`は成功、`tests/lib/photos`+
  `tests/hooks/usePhotoCapture.test.ts`(151件)は全てパス。**iPad実機での正面・
  左45°・右45°のガイド表示、シャッター・ゴースト調整UIとの重なりの見え方はまだ
  確認できていない。**

**この解除は上記のPart 2(顔位置合わせガイドの形状変更)のみに限る。** 続くPart 3
(お客様モードの写真追加)は別途着手する。5タブ構成・TL-5構成・AI提案の会話トーン・
LINE領域・admin領域については引き続き凍結を継続する。

### v1.0.1 着手済み事項（KarteMemoSection.tsx「カルテメモ」入力欄の拡大のみ・2026-09-19ユーザー承認）

カルテメモの表示・入力領域が狭いとの指摘を受け、`KarteMemoSection.tsx`内の新規追加・編集用
`<textarea>`(2箇所)の高さのみを拡大する範囲に限り、iPadカルテ画面の凍結を解除した。

- 両`<textarea>`の`rows`を4→10へ、`minHeight`を新規追加(220px)。他のprops(`resize:vertical`
  等)・保存/更新ロジック・APIは無変更。
- カード自体(`Card`コンポーネント)にはmaxHeight/overflow制約が無く、保存済みメモの表示側
  (`<p>`要素)も元々可変長で全文表示のため、変更対象は入力用`<textarea>`のみとした。
- 検証: `npm run typecheck`に新規エラーなし(既存の別作業由来のエラーとは無関係であることを
  確認済み)。Playwright(iPad想定ビューポート1024×1366、本番Supabase・実顧客「小宮山 仁美」、
  検証用一時spec`e2e/karte-memo-size-verify.spec.ts`はコミット前に削除)で、拡大後の
  `<textarea>`が約410×290pxで表示されることを確認した(保存は行わずキャンセルで終了、
  テストデータは作成していない)。

**この解除は上記の入力欄拡大のみに限る。** 5タブ構成・TL-5構成・AI提案の会話トーン・
LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き
凍結を継続する。

### v1.0.1 着手済み事項（写真撮影画質改善 Phase 3-A 取りこぼし是正のみ・2026-09-20ユーザー承認）

小宮山様の「過去の写真」表示件数不一致の調査中に派生した「9/18分の写真だけ極端にファイル
サイズが小さい」という疑問について、READ ONLY調査(本番DB・Storageの実データ照会)の結果、
「選択して追加」経路(`fileToWebpBlob.ts`の`convertImageFileToWebpBlob`)だけがPhase 3-A
(上記エントリ、2026-09-18)の品質引き上げ(0.8→0.9)から漏れていたことが判明した。同関数は
`encodeCanvasWithFallback`へquality引数を明示的に渡すため、共有関数側のデフォルトが0.9に
上がっても独自デフォルト値(0.8)で上書きされ、ライブ撮影(`captureFrame.ts`)側だけ品質改善が
適用され、ファイル選択側は取り残されていた。

- `src/lib/photos/fileToWebpBlob.ts`: `convertImageFileToWebpBlob`のデフォルト引数を
  `quality = 0.8` → `quality = 0.9` へ変更(1行)。呼び出し元(`usePhotoCapture.ts`の
  `captureFromFile()`)は明示的な引数を渡していないため、常にこのデフォルト値が使われる。
  解像度側の上限(`MAX_CAPTURE_LONG_EDGE_PX`)は`captureFrame.ts`からの参照を共有しており
  Phase 3-Aで既に3072pxへ追従済みのため変更不要(今回変更したのは品質のみ)。
- 併せて、小宮山様の9/18データの主因はこの品質差(0.8/0.9)ではなく、5枚が480×640という
  解像度そのものが小さい既存ファイル(練習用の人形写真)を「選択して追加」で登録した結果と
  判明した(ライブ撮影経路の実測解像度は1440×1920で統一)。この5枚は削除対象のテストデータ
  として扱う(別途ユーザー指示に基づき削除予定、削除自体はこのエントリの範囲外)。
- **実機未確認**: 本修正後、実際に「選択して追加」で登録した写真のファイルサイズ・画質が
  改善されるかは未検証(jsdomでは検証不可な領域、`fileToWebpBlob.ts`冒頭のコメント参照)。

**この解除は上記`convertImageFileToWebpBlob`のデフォルト品質変更のみに限る。** 5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ
画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（iPadスタッフカルテ「前回メモをワンタップ参照」機能の新設のみ・2026-09-20ユーザー承認）

カルテメモ入力時に前回来店時の記録を確認できるよう、`KarteMemoSection.tsx`(iPadスタッフ
カルテ左カラム)のメモ一覧と「カルテメモを追加」ボタンの間に、デフォルト閉じた折りたたみ
「▸ 前回の記録を見る」を新設した(PHASE IPAD-PREV-MEMO-1)。「前回」は本日のvisitを除いた
直近の来店(`ipadKarteData.ts`の`todayVisit`算出と同じ考え方: 今日のvisitがあれば
`visits[1]`、無ければ`visits[0]`)。

- `src/components/customer/ipadKarte/ipadKarteData.ts`: `IpadKarteData`に
  `previousVisitDate`・`previousMenuName`・`previousTreatmentMemo`を追加。前回visitの
  `treatment_memo`のみ既存API(`/api/customers/[id]/visits/[visitId]/treatment`)経由で
  追加取得する(新規API・新規DB列は無し。今日のvisit向けに既にある取得パターンをそのまま
  前回visit向けに複製しただけ)。取得失敗時は他の表示に影響させない(try/catchでnullに留める、
  既存の今日分と同じ方針)。
- `src/components/customer/ipadKarte/IpadStaffKarteView.tsx`: 上記3値を
  `<KarteMemoSection>`へpropsとして渡すよう1箇所変更。
- `src/components/customer/ipadKarte/KarteMemoSection.tsx`: 折りたたみUIを新設。展開時に
  「前回の施術メモ」(`previousTreatmentMemo`、無ければ「記録がありません」)・
  「前回のカルテメモ」(`memos`の中で本日作成分を除いた直近1件、無ければ「記録がありません」)
  をそのまま表示する。**「注意点」は独立フィールドを設けず、これら2つの自由記述文に
  含まれている前提とした(2026-09-20ユーザー承認、既存の「⚠ 重要事項」カード(禁忌事項)は
  常時表示でカバー済みのため折りたたみ内への再掲は行わない)。**「前回のカルテメモ」は
  「今日すでに書いた分があれば除外し、その前の1件を表示する」仕様(2026-09-20ユーザー承認:
  施術中に今日のメモを書き込んでも参照データが「前回来店時のメモ」であり続けるようにする
  ため)。前回来店・前回カルテメモ・前回施術メモのいずれも無い顧客ではボタン自体を出さない。
- 検証: `npm run typecheck`に新規エラーなし。Playwright(本番Supabase・実顧客
  「小宮山 仁美」、検証用一時spec`e2e/prev-memo-verify.spec.ts`はコミット前に削除)で、
  ①ボタンがデフォルト閉じた状態で表示されること、②タップで展開し前回来店日
  (2026年9月15日)・メニュー名が見出しに表示されること、③前回のカルテメモ欄に
  検証用に一時投入したテストメモ(検証後にDBから削除済み)が表示されること、④施術メモが
  無い場合に「記録がありません」と表示されることを確認した。

**この解除は上記「前回の記録を見る」折りたたみの新設のみに限る。** 5タブ構成・TL-5構成・
AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様
については引き続き凍結を継続する。

### v1.0.1 着手済み事項（お客様モード「比較｜拡大」モード切替の廃止＋比較モードへのピンチズーム追加のみ・2026-09-20ユーザー承認）

お客様モード写真カルテの「比較｜拡大」セグメンテッドコントロールを削除し、比較モード
(2枚並び表示)を唯一の表示モードにした。拡大表示自体は既存のライトボックス
(写真タップ→全画面表示、旧実装は静止画のみ)にピンチズーム機能を移設する形で維持する。

- `src/components/customer/guestMode/CustomerModeView.tsx`:
  - `photoMode`state・「比較｜拡大」セグメンテッドコントロールUIを削除。比較モードの
    ショートカット(前回↔今回・初回↔今回・🔀自由選択)は常時表示に変更。
  - 拡大モード専用だった state・算出値(`enlargeOccasionKey`・`enlargeFallbackTab`・
    `extraPhotoUrls`・`enlargedOccasion`・`enlargedPhoto`・`enlargedUrl`・
    `enlargedLoading`・`enlargedActiveVisitId`)と、それらに依存する「初回/前回
    ショートカット(拡大モード用)」「全来店日リストタブ」UIを削除。
  - 「過去の写真」サムネイルタップ時の遷移先を、旧`openOccasionInEnlargeMode`(拡大モードへ
    切替)から新設の`openOccasionInLightbox`(ライトボックスを直接開く、既存の
    `lightboxUrl`stateを再利用)に置き換え。
  - `EnlargedPhotoPanel`(拡大モード専用の単独写真表示、`usePinchZoom`使用)を削除し、
    そのピンチズーム機構(`usePinchZoom`の呼び出し・transform適用ロジックは無変更で移設)を
    ライトボックス用に新設した`ZoomableLightboxImage`へ持たせた。ライトボックスの背景
    クリックで閉じる挙動と競合しないよう、画像コンテナに`stopPropagation`を追加。
  - `usePinchZoom.ts`自体(ピンチ/pan/ダブルタップリセットのロジック)は無変更。
  - IpadStaffKarteView側のPhotoPanel呼び出し・比較ロジック・API・DBには一切手を加えていない
    (お客様モード内のみのUI変更)。
- `src/components/customer/guestMode/customerModeData.ts`: 拡大モードの「全来店日リストから
  選択」専用だった`visitTabs`(および`buildVisitTabs`の呼び出し)を削除(呼び出し元が
  無くなったため)。`buildVisitTabs`/`VisitTab`自体(`src/lib/photos/timelineGrouping.ts`)は
  ファイルとして保全(他機能からの呼び出しはなし、将来の再利用に備え削除しない)。
- 検証: `npm run typecheck`に新規エラーなし。Playwright(本番Supabase・実顧客
  「小宮山 仁美」、検証用一時spec`e2e/enlarge-mode-removal-verify.spec.ts`はコミット前に
  削除)で、①「比較」「拡大」ボタンが存在しないこと、②比較モード(今回の写真)が常時表示
  されること、③写真の拡大ボタンタップでライトボックスが開くこと、④CDP経由のタッチ
  イベント(2点タッチを広げる)でライトボックス内画像のtransformが`scale(4)`(最大倍率)まで
  変化することを確認した(実機でのタップ操作でのピンチ自体は未確認、Chromiumの合成タッチ
  イベントでの検証)。

**この解除は上記「比較｜拡大」モード切替の廃止・比較モードへのピンチズーム追加のみに限る。**
5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/
iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（お客様モード「終了」ボタンの長押し化のみ・2026-09-20ユーザー承認）

お客様に見せている画面(CustomerModeView)から誤タップでスタッフ専用画面
(CustomerBottomSheet)へ戻ってしまうのを防ぐため、「終了」ボタンを長押し(600ms)でのみ
反応するよう変更した。調査の結果、「お客様モード⇄スタッフカルテ」を直接切り替える単一の
ボタンは存在せず、CustomerBottomSheetのヘッダーにある独立した2ボタン(「✿ お客様モード」・
「🗂 iPadカルテ(β)」)とCustomerModeView右上の「終了」ボタンのみが実在の切替導線と判明
した。ユーザー確認の結果、「✿ お客様モード」ボタンは即座に見せたいため従来通りワンタップの
まま、「🗂 iPadカルテ(β)」ボタンも従来通りワンタップのまま(スタッフの操作性優先)とし、
**「終了」ボタンのみ**が対象と確定した。

- `src/components/customer/guestMode/useLongPress.ts`(新規): Pointer Eventsのみで実装した
  長押し検出フック(外部ライブラリ追加なし、`usePinchZoom.ts`と同じ方針)。判定時間(既定
  600ms)未満でpointerup/leave/cancelした場合は何も起きず、`progress`(0〜1)を視覚
  フィードバック用に返す。
- `src/components/customer/guestMode/CustomerModeView.tsx`: 「終了」ボタンの`onClick`を
  廃止し、`useLongPress(onClose)`のハンドラを紐付け。押している間はボタン背景をgoldに
  変え、周囲に進捗リング(SVG circle、strokeDashoffsetで表現)を表示する。ボタン下に
  常時「長押しで終了」ヒントテキストを追加し、通常タップでは反応しないことを事前に
  分かるようにした。`aria-label`も「お客様モードを終了」→「長押しして終了」に変更。
- 検証: `npm run typecheck`に新規エラーなし。Playwright(本番Supabase・実顧客
  「小宮山 仁美」、検証用一時spec`e2e/exit-longpress-verify.spec.ts`はコミット前に削除)で
  検証。**iPad想定幅(820px)では**①ヒント表示、②200ms未満の押下では閉じないこと、
  ③押している間ボタン背景がgoldになること、④700ms以上の押下でお客様モードが閉じる
  (CustomerBottomSheetに戻る)ことを実マウスdown/upイベントで確認した。
- **別件として発見した既存の問題(今回のスコープ外・未修正)**: デフォルトのE2Eビューポート
  (390px・スマホ想定)では、ヘッダー中央の見出し文言(`white-space:nowrap`)が折り返さず
  横方向にはみ出し、「終了」ボタンが画面幅の外(x座標が390pxを超える位置)に押し出されて
  タップできなくなる現象を確認した(本機能の変更前から存在する既存レイアウト、今回は
  一切手を加えていない)。iPad幅(768px以上)では発生しない。スマホでの利用実態次第では
  別途対応要否の判断が必要(ユーザー未確認)。

**この解除は上記「終了」ボタンの長押し化のみに限る。** 5タブ構成・TL-5構成・AI提案の
会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については
引き続き凍結を継続する。

### v1.0.1 着手済み事項（iPad店舗共通ログイン+担当者タグ選択の新設のみ・2026-09-20ユーザー承認）

iPad用の店舗共通ログインアカウントを新規作成し、顧客のカルテを開いた際に担当スタッフを
タグとして選択、そのセッション中(端末単位・2時間保持)のstaffIdとして写真登録・カルテメモの
書き込みに紐付ける機能を新設した(PHASE IPAD-SHARED-LOGIN-1)。個人ログイン時は一切無変更
(担当者タグUI自体が表示されない)。事前にREAD ONLY調査→設計案提示→久保田さん承認を経て
実装した。

- **店舗共通アカウント**: `auth.users`1件(email: `ipad@salon-riora.jp`。作成時は
  `ipad-shared@salon-riora.jp`だったが、2026-09-20ユーザー承認によりSupabase Auth Admin API
  経由でこのメールアドレスへ変更済み。UUID・パスワード・`SHARED_IPAD_STAFF_USER_ID`は変更なし)
  +`brain_staff`1件(id=`a75826c5-5327-4a8b-867c-
  8c4bfbfcbafb`、user_id=`7e29dcfb-20ce-4f7c-8793-6b46b2f2aa60`)を
  `auth.admin.createUser()`+SQL insertで作成済み(既存のスタッフ招待フローは使わず、
  仮パスワードで即座に使える状態にする指示のため)。
- `src/lib/constants.ts`: `SHARED_IPAD_STAFF_USER_ID`定数を追加(共通アカウントの
  `auth.users.id`。`DEMO_STORE_ID`と同じ「クライアントに含まれてよい公開識別子」方針)。
- **新規API** `GET /api/staff/active-list`: 担当者タグ候補一覧(`brain_staff`の
  `is_active=true`・共通アカウント自身を除く`id, name`)を返す。既存の`/api/admin/staff`は
  管理者専用のため共通アカウントからは使えず、新規に軽量版を追加した。
- **新規lib** `src/lib/staffTag/`: `useStaffTagSession.ts`(localStorageに選択結果+選択時刻を
  保存し2時間TTLで判定するクライアントhook)・`staffTagApiClient.ts`(上記APIの薄い
  クライアント)・`resolveStaffIdOverride.ts`(サーバー専用。**リクエストのJWTが
  `SHARED_IPAD_STAFF_USER_ID`本人の場合のみ**、指定された`staffId`を有効な`brain_staff`か
  検証した上で許可する。個人ログイン時は指定されても常に無視され、なりすましは発生しない)。
- **新規UI** `src/components/customer/ipadKarte/StaffTagBar.tsx`: 店舗共通ログイン時のみ
  意味を持つ表示専用コンポーネント。担当未選択(または2時間経過)ならフルスクリーンの
  選択プロンプトをブロッキング表示、選択済みなら「担当: ◯◯」チップを表示しタップで
  いつでも選び直せる。`IpadStaffKarteView.tsx`・`CustomerModeView.tsx`の両方のヘッダーに
  設置した(写真登録の導線がPHASE GUEST-MODE-PHOTO-MOVE-1でお客様モード側にあるため。
  お客様の業務データを一切扱わない表示専用コンポーネントのため、CustomerModeView.tsxの
  「スタッフ専用コンポーネントは読み込まない」方針には抵触しないと判断)。
- **write API側の変更**: `POST /api/customer-karte-memos`・`POST /api/customers/[id]/photos`
  が任意の`staff_id`/`staffId`上書きを受け付けるよう変更。`resolveStaffIdOverride`経由で
  検証した場合のみ反映する。`customer_karte_memos.staff_id`(auth.users.id参照)と
  `brain_customer_photos.created_by`(brain_staff.id参照)でスキーマの参照先が異なるため、
  `resolveStaffIdOverride`は両方の値(`staffBrainId`・`authUserId`)を返し、呼び出し元が
  自分のテーブルに合う方を使う。
- **クライアント側の配線**: `KarteMemoSection.tsx`(`staffIdOverride`prop追加)・
  `captureConfirmFlow.ts`の`CapturedPhotoPayload`・`usePhotoCapture.ts`の
  `UsePhotoCaptureOptions`・`photoApiClient.ts`の`uploadCustomerPhoto`・
  `IpadPhotoCaptureModal.tsx`(`staffId`prop追加)を順に配線し、選択済みの担当者タグが
  写真登録(撮影・選択して追加どちらも同じ`beginReview`を通るため両方に効く)・カルテメモ
  登録の両方に反映されるようにした。
- 検証: `npm run typecheck`に新規エラーなし。本番環境で直接API検証(①共通アカウントで
  `GET /api/staff/active-list`が共通アカウント自身を除いた4名を返す、②共通アカウントから
  `staff_id`上書き付きでカルテメモ・写真を登録すると実際に指定した担当者のID
  (`customer_karte_memos.staff_id`=対象のuser_id、`brain_customer_photos.created_by`=
  対象のbrain_staff.id)で保存される、③個人(admin)ログインから同じ上書きを試みても無視され
  本人の識別子のまま保存される、をSQLで直接確認済み)。Playwright(本番Supabase・実顧客
  「小宮山 仁美」、検証用一時spec`e2e/staff-tag-verify.spec.ts`はコミット前に削除)で、
  ④共通アカウントで開くと選択プロンプトが出ること、⑤選択後チップ表示に変わること、
  ⑥別の顧客を開いても(2時間以内のため)再表示されずチップが保持されること、⑦チップタップで
  選び直せること、⑧個人(admin)ログインではプロンプト・チップとも一切表示されないことを
  確認した。テスト用に作成したカルテメモ・写真はいずれも検証後に削除済み。
- **未実施**: アイドルタイムアウト・重要操作PIN(2026-09-20ユーザー承認により対象外)。
- **要確認事項(久保田さんへ)**: 共通アカウントのパスワードは仮値のため、実運用開始前に
  Supabase管理画面等で正式なパスワードへ変更する必要がある(この会話内で個別にお伝え済み、
  ファイルには平文パスワードを一切残していない)。

**この解除は上記「iPad店舗共通ログイン+担当者タグ選択」機能の新設のみに限る。** 5タブ構成・
TL-5構成・AI提案の会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ
画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（iPad専用カルテ入口 `/karte` の新設のみ・2026-09-20ユーザー承認）

スマホ用画面(5タブ構成)とiPad専用カルテ画面を、ログイン後の入口から明確に分離する機能を
新設した(PHASE IPAD-KARTE-ENTRY-1)。設計書(`docs/IPAD_KARTE_ENTRY_ROUTE_DESIGN_v1.md`
v1.2)・凍結解除の事前承認記録(`docs/IPAD_KARTE_ENTRY_ROUTE_FREEZE_APPROVAL_v1.md`)を
経て、記録6節で承認された9ファイルのみを対象に実装した。

- **新規**: `app/karte/page.tsx`・`app/karte/[customerId]/page.tsx`・
  `src/components/karte/KarteEntryScreen.tsx`(本日の予約+顧客検索、5タブナビ・
  「通常画面へ」リンクなし)・`src/components/karte/KarteCustomerSwitcher.tsx`
  (`GET /api/customers/[id]`で顧客を直接取得し、2択ランディングを介さず常に
  `CustomerModeView`から開始する薄いラッパー)。
- **変更**: `app/login/page.tsx`・`app/page.tsx` — 既存のadmin/個人ログイン分岐は無変更のまま、
  店舗共通ログイン(`SHARED_IPAD_STAFF_USER_ID`)のみ`/karte`へ着地する分岐を追加。
  `/phase1`のリネーム・`/staff`ルートの新設は行っていない(既存7ファイルへの影響を避ける
  非破壊方針を優先、`docs/IPAD_KARTE_ENTRY_ROUTE_DESIGN_v1.md`1-1節参照)。
- **変更**: `src/components/customer/guestMode/useLongPress.ts` — 第3引数
  `options?: { moveCancelPx?: number }`を追加し、pointerdown位置から指定px以上動いたら
  自動キャンセルする機能を実装。**完全にオプトイン**(`options`未指定の既存呼び出し元は
  一切動作が変わらない)。
- **変更**: `src/components/customer/guestMode/CustomerModeView.tsx` — 任意prop
  `onSwitchToStaffView?: () => void`を追加。指定時のみヘッダーの「Salon Riora」ロゴに
  `useLongPress(onSwitchToStaffView, 1000, { moveCancelPx: 12 })`を紐付け、1000ms長押し
  (誤発動防止の移動キャンセル付き)でスタッフ用カルテへ切り替える。押下中はロゴの色が
  白+goldの背景に変化し、進捗バーを表示する。未指定時(CustomerBottomSheet経由の既存呼び出し)
  はロゴは従来通り非インタラクティブな装飾のまま。
- **変更**: `src/components/customer/ipadKarte/IpadStaffKarteView.tsx` — 任意prop
  `onSwitchToCustomerView?: () => void`を追加。指定時のみヘッダー左上・ロゴ付近に
  常時表示の「お客様用カルテへ戻る」ボタンを表示し、通常タップで即座に呼び出す
  (長押し不要、ブラウザの戻る操作には依存しない)。未指定時は何も表示されない。
- 検証: `npm run typecheck`に新規エラーなし。Playwright(本番Supabase・実顧客
  「小宮山 仁美」、検証用一時spec`e2e/karte-entry-verify.spec.ts`はコミット前に削除)で、
  凍結解除事前承認記録のテスト計画12項目(①店舗共通ログイン→`/karte`自動着地、
  ②個人ログイン→`/phase1`のまま(回帰確認)、③5タブ・「通常画面へ」リンクが無いこと、
  ④顧客選択で2択ランディングを介さずお客様用カルテが直接開くこと、
  ⑤`/karte/[customerId]`直接アクセスでの顧客取得、⑥1000ms未満のタップでは切り替わらない
  こと、⑦1000ms以上の長押しでスタッフ用カルテへ切り替わり視覚フィードバックが出ること、
  ⑧長押し中の移動でキャンセルされること、⑨「お客様用カルテへ戻る」が通常タップで即反応
  すること、⑩担当者タグ選択が機能すること、⑪CustomerBottomSheet経由の既存呼び出し
  (【4】の「終了」ボタン600ms長押しを含む)に回帰が無いこと、⑫typecheck)を全て確認した。

**この解除は上記`/karte`関連の新規4ファイルおよび5ファイルへの追加変更のみに限る。**
5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・admin領域、
`CustomerBottomSheet.tsx`・`CustomersScreen.tsx`・`Phase1Screen.tsx`・
`AppBottomNav.tsx`・`app/phase1/page.tsx`・`app/customers/page.tsx`・
`app/home/page.tsx`、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については
引き続き凍結を継続する。

### v1.0.1 着手済み事項（画面切り替え・初回読み込み速度改善 Step 1: 長押し切り替えの再フェッチ防止＋治療メモ/signed URL並列化・2026-09-21ユーザー承認）

「画面切り替え・初回読み込みが遅い」というユーザー報告を受けたREAD ONLY調査(同日実施)の
結果判明した根本原因のうち、影響範囲が最小かつ効果の大きい2点についてユーザー承認を得て
以下の範囲に限り凍結を解除した。認証・認可の重複コスト(全APIが`extractStaffFromRequest`+
`canAccessCustomer`を毎回individually実行している件)等の残り施策は今回のスコープ外。

- **①`KarteCustomerSwitcher.tsx`(長押し切り替えの再フェッチ防止・PERF-KARTE-SWITCH-CACHE-1)**:
  従来は`mode === 'staff' ? <IpadStaffKarteView> : <CustomerModeView>`という条件分岐で、
  長押し切り替えのたびに片方を完全にアンマウント/リマウントしており、同じ顧客のデータでも
  `useIpadKarteData`/`useCustomerModeData`が毎回ゼロから再フェッチしていた(これが「長押し
  切り替えが遅い」の直接原因と判明)。これを、`CustomerModeView`は従来通り常時マウント、
  `IpadStaffKarteView`は**初回切り替え時に一度だけ遅延マウントし、以降は顧客が変わるまで
  アンマウントせず`display:none`/`display:contents`で表示を切り替えるだけ**にする方式へ変更した。
  両コンポーネントのルート要素が元々`position:fixed; inset:0`のため、ラップ用divの
  `display`切替だけで表示位置・レイアウトへの副作用なく実現できた。顧客が変わった場合
  (`customerId`変更)は`staffViewMounted`をfalseに戻し、次の顧客では再び初回切替時に
  遅延マウントする(古い顧客のデータを握ったまま次の顧客画面に持ち越さないため)。
  `CustomerModeView`/`IpadStaffKarteView`自体のロジック・見た目・props構成は無変更
  (呼び出し側のマウント方式のみの変更)。
- **②`ipadKarteData.ts`の直列await解消(PERF-KARTE-PARALLEL-1)**: 最初の`Promise.all`
  (写真一覧・来店履歴・肌記録・目標・禁忌事項・ホームケア商品の6件)の後、「今日の施術メモ」
  →「前回の施術メモ」→「写真signed URL取得」の3つを直列awaitしていたが、いずれも最初の
  `Promise.all`で確定済みの値(`todayVisitId`・`previousVisit`・`photos`)のみに依存し
  互いには依存しないため、3つとも`Promise.all`で並列実行するよう変更した。各fetch内の
  try/catchによるフォールバック方針(取得失敗しても他の表示に影響させない)は無変更。
- 検証: `npm run typecheck`は両ファイルとも新規エラーなし(既存の別作業由来のエラー
  (voice memo再実装・DashboardRepo等、未コミットの別作業)とは無関係であることを、
  変更前後で同一のエラー集合であることを確認して検証済み)。Playwright(本番Supabase・
  実顧客「小宮山 仁美」、検証用一時spec`e2e/karte-switch-perf-verify.spec.ts`はコミット前に
  削除済み)で、(a)お客様モードのみの段階ではスタッフ側API(goal・treatment)が未発火である
  こと(遅延マウントの確認)、(b)スタッフ用カルテへの1回目の長押し切り替えでgoal/
  skin-records/homecare-products/contraindications/visit-historyが発火すること、
  (c)お客様用カルテへ戻って**再度**スタッフ用カルテへ切り替えても、これら全エンドポイントの
  リクエスト件数が1回目から**一切増加しない**こと(再フェッチが起きていないことの直接確認)、
  (d)「今日/前回の施術メモ」と「写真signed URL」が同一ミリ秒で発火し並列実行されている
  ことを確認した。往復中のpage error・console errorも無いことを確認済み。
- **トレードオフの明記**: 今回は「初回切り替え時のみ遅延マウント」方式を採用したため、
  スタッフ用カルテを一度も開かない顧客ではスタッフ側APIは一切発火しない(初回表示の負荷を
  増やさない)。一方、スタッフ用カルテを一度でも開いた後は、その顧客を表示している間は
  ずっとメモリ上に保持され続ける(顧客を切り替えるまでアンマウントしない)。

**この解除は上記①②の変更のみに限る。** 認証・認可の重複コスト削減・写真サムネイルの
リサイズ配信・写真一覧APIのDBクエリ統合等、調査報告で提示したその他の改善案は今回の
スコープ外(別途相談の上で着手する)。5タブ構成・TL-5構成・AI提案の会話トーン・LINE領域・
admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については引き続き凍結を継続する。

### v1.0.1 着手済み事項（顔シェーマ機能の新設・2026-09-21ユーザー承認・Phase 0〜7完了）

iPadスタッフカルテに、顔正面イラストの上へニキビ・赤み・毛穴・HIFU範囲を描き込んで記録する
「顔シェーマ」機能を新設した。以前この機能をREAD ONLYで検討したセッションがあったとの
ことだったが、リポジトリ・Git履歴・Artifact・Google Drive・永続メモリのいずれにも記録が
残っておらず参照できなかったため、ユーザーが確定した仕様(下記)を要件として、既存の
写真カルテ機能(`brain_customer_photos`)のアーキテクチャ・規約を最大限踏襲しゼロベースで
設計・段階的に実装した(Phase 0設計→Phase 1 DB→Phase 2純粋関数→Phase 3 API→Phase 4
キャンバスフック→Phase 5スタッフUI→Phase 6お客様UI→Phase 7自動連携、各Phaseごとに
ユーザー承認を得てから次へ進む運用で実施)。

**確定仕様**: A テンプレートは正面のみ(`IMG_1381.JPG`ベース)。B カテゴリ別描画
(ニキビ=ポイント・赤み/毛穴=エリア・HIFU照射=線引き、実施/回避はスタイル違い(実線/破線)
で表現し独立カテゴリにはしない、凡例を自動表示)。C Undo・クリア・Pointer Eventsによる
パームリジェクション。D `visit_id`紐付け(null許容、写真機能と同じ「撮影機会」対策を踏襲)、
ストローク座標(JSON、画像化しない)で保存し再編集・前回比較可能。E iPadスタッフカルテ
(編集用)・お客様モード(閲覧用)の両方に表示。

#### DBテーブル: `brain_customer_facial_schemas`

マイグレーション: `supabase/migrations/20260921000000_brain_customer_facial_schemas.sql`
(本番適用済み・Phase 1)。`brain_customer_photos`と同じ「service_role限定RLS」方式
(SELECT/INSERT/UPDATE/DELETEとも`service_role`のみに許可するポリシーを明示作成、
`authenticated`/`anon`には一切許可しない default-deny)。

- `id`/`store_id`/`customer_id`(→`brain_customers`)/`visit_id`(→`brain_visits`、null許容)/
  `schema_date`(date、JST暦日)/`template_key`(text、既定`'face_front'`、将来の複数テンプレート
  拡張余地)/`strokes_data`(jsonb、既定`{"version":1,"strokes":[]}`)/`created_by`・
  `updated_by`(→`brain_staff`)/`created_at`/`updated_at`/`deleted_at`。
- 写真機能との意図的な差分: 写真は撮影のたびに新規INSERT(常に追記型)だが、顔シェーマは
  同じ機会を保存後も描き直し続ける性質があるため、**customer_id×撮影機会(visit_idまたは
  schema_date)につき1行をUPSERTする**設計。部分ユニークインデックス
  `ux_facial_schemas_customer_visit`(`visit_id IS NOT NULL`時)・
  `ux_facial_schemas_customer_date`(`visit_id IS NULL`時、customer_id×schema_date)で
  1機会=1行を保証する。
- 座標は**テンプレート画像に対する正規化座標(0〜1)**で保存する(スタッフ・お客様の両画面で
  canvas実寸が異なっても矛盾なく再現できるようにするため)。カテゴリの色・線種は保存データに
  焼き込まず、`facialSchemaCategories.ts`で描画時に都度解決する(将来調整しても過去データの
  意味が変わらない、`bodyPartLabel()`と同じ思想)。

#### 純粋ロジック層: `src/lib/facialSchema/`(Phase 2・DOM/Reactに非依存、ユニットテスト対象)

- `facialSchemaCategories.ts`: カテゴリ×色×線種×ツールの定義(ニキビ=赤丸/point、
  赤み=ピンク半透明/area、毛穴=アンバー半透明/area、HIFU実施=青実線/line、
  HIFU回避=オレンジ破線/line。**いずれも暫定値**、将来の見た目調整はこのファイルのみで完結)。
- `strokeModel.ts`: `Stroke`/`StrokesData`型、`addStroke`/`undoLastStroke`/`clearStrokes`
  (すべてイミュータブル)、`parseStrokesData`(version不一致・不正データを安全にフォールバック、
  個々の壊れたストローク/座標点のみを除外)。
- `facialSchemaSelection.ts`: 写真機能の`comparisonSelection.ts`/`ghostSelection.ts`と同じ
  「撮影機会(occasion)」の考え方(`visit_id`があれば`visit:${id}`、無ければ日付キー)を再利用。
  写真と異なりschema_dateはDBの実カラム(JST暦日)であるため、`toJstDateStr`/
  `todayJstDateStr`(+9時間固定オフセット、`linkPhotosToVisit.ts`と同じ方針)で正確なJST日付を
  算出し、`excludingCurrentOccasion`・`buildPreviousSchema`(前回記録の抽出)を提供する。
- `canvasRenderer.ts`: `renderStrokes(ctx, strokes, box)` — 正規化座標をpx変換してcanvas
  2Dコンテキストへ描画する。DOM非依存のインターフェース(`RenderCanvasContext`)経由で
  実DOMのcanvasにもテスト用モックにも渡せる(`captureFrame.ts`と同じ設計)。
- `useFacialSchemaCanvas.ts`(Phase 4、Reactフック): Pointer Events
  (onPointerDown/Move/Up/Cancel)を自前実装(外部ライブラリ追加なし、`usePinchZoom.ts`/
  `useLongPress.ts`と同じ方針)。**パームリジェクション**: ①Apple Pencil(`pointerType==='pen'`)
  描画中は他の`touch`入力を常に無視(ペン優先)、②指のみで描画中に別の`touch`が増えたら
  手のひら疑いとして進行中のストロークをキャンセル。判定ロジック(`decidePointerDown`/
  `decidePointerEnd`)・座標正規化(`toNormalizedPoint`/`clamp01`)は純粋関数としてexportし、
  フック本体をレンダリングせずテストできるようにしている(このプロジェクトには
  `@testing-library/react`等が無くフックを直接レンダリングしてテストできないため、
  `usePhotoCapture.ts`の`CAMERA_CONSTRAINTS`と同じ回避パターンを踏襲)。
- `linkFacialSchemasToVisit.ts`(Phase 7): `linkPhotosToVisit.ts`と同じ「visit解決直後の
  非致命的な下流紐付け処理」。`schema_date`が既にJST暦日の`date`型のため、写真のような
  タイムゾーン範囲変換は不要で直接等価比較でよい。

#### API: `app/api/customers/[id]/facial-schemas/`(Phase 3)

写真カルテAPIと同じ`extractStaffFromRequest`→`canAccessCustomer`→(PUTのみ)
`verifyVisitBelongsToCustomer`(`src/lib/photos/ownership.ts`を再利用)→
`resolveStaffIdOverride`の認証・認可チェーンを適用する。

- `GET /api/customers/[id]/facial-schemas`: 履歴一覧(`schema_date DESC`)。
- `PUT /api/customers/[id]/facial-schemas`: 現在の機会(`visitId`があればそれ、無ければ
  本日JST日付)へのupsert。部分ユニークインデックス競合(Postgres `23505`)時は既存行を
  再取得してUPDATEへフォールバックする(`commitCustomerPhoto.ts`の競合処理と同じ考え方)。
- `DELETE /api/customers/[id]/facial-schemas/[schemaId]`: ソフトデリート。所有権不一致は
  404ではなく403(`customer-memories`/写真の既存慣行)。
- 所有権チェックは`src/lib/facialSchema/verifyFacialSchemaOwnership.ts`
  (`verifyPhotoOwnership`と同じ思想の専用モジュール)。

#### コンポーネント構成

- `src/components/customer/shared/FacialSchemaKit.tsx`(Phase 5/6共有): テンプレート画像
  定数、`FacialSchemaLegend`(凡例自動表示)、`FacialSchemaThumbnail`(読み取り専用の静止画
  レンダリング、スタッフ側「前回のシェーマ」プレビューとお客様側閲覧の両方で共用)。
- `src/components/customer/ipadKarte/FacialSchemaSection.tsx`(Phase 5、編集用):
  `IpadStaffKarteView.tsx`の左カラム、`<KarteMemoSection>`直後に配置。カテゴリ切替・凡例・
  描画キャンバス(ResizeObserverで実寸追従・高DPI対応)・元に戻す/全消去/保存ボタン・
  「前回のシェーマを読み込む(コピー)」機能を持つ。保存成功後は履歴を自動再取得する。
- `src/components/customer/guestMode/FacialSchemaViewer.tsx`(Phase 6、閲覧用):
  `CustomerModeView.tsx`下部のCardスタック(「過去の写真」の直後・「来店履歴」の直前)に配置。
  完全読み取り専用(pointerハンドラ・編集UIを一切持たない)。CustomerModeViewの設計方針どおり
  自前で`GET .../facial-schemas`をfetchする自己完結コンポーネント。今回(最新)と前回を
  横並びで比較表示(記録が1件のみの場合は今回のみ1カラム、0件なら記録が存在するまでカード
  自体を表示しない)。
- 写真の「ゴースト機能」で繰り返し発生した位置合わせの苦労(5回の改修を要した)は、顔シェーマ
  では原理的に発生しない: 常に同一の固定テンプレート画像の上に描くため、「今回」と「前回」は
  最初から同じ座標系で完全に一致し、特別な位置合わせロジックが不要。

#### 検証結果(全Phase共通)

各Phaseで`npm run typecheck`・関連ユニットテスト・`npm run build`を実行しパス済み
(顔シェーマ関連の新規ユニット/APIテストは合計100件超、既存テストへのリグレッション無し)。
Phase 5・6のUI実機相当確認はPlaywright(本番Supabase・実顧客「小宮山 仁美」)で実施したが、
**開発サーバー(`npm run dev`)ではFast Refresh(HMR)の介入により描画状態が失われ
テストが不安定になる**ことが判明したため、`npm run build`+`npm start`(本番ビルド)に対して
検証する方式に切り替えた。検証用に作成したテストデータ・一時specはすべて削除済み。

**この解除は上記の顔シェーマ機能新設(DBテーブル・API・純粋ロジック・キャンバスフック・
スタッフ/お客様UI・接客ログ保存時の自動紐付け)に限る。** 5タブ構成・TL-5構成・AI提案の
会話トーン・LINE領域・admin領域、およびそれ以外の顧客タブ/iPadカルテ画面の仕様については
引き続き凍結を継続する。

### v1.0.1 着手済み事項（顧客トップページ新設・詳細ページ調整のみ・2026-09-24ユーザー承認）

「本日の予約」「顧客タブ検索結果」でお客様名をタップした際の遷移先を、現行の
`CustomerBottomSheet`直接表示から新設の「顧客トップページ」へ差し替え、そこから
「詳細ページ」(`/karte/[customerId]`、`KarteCustomerSwitcher.tsx`)へ進む2段階構成にした。
写真4アングル表示・撮影日付表示は今回の指示で明示的に保留(スコープ外)とされたため未実装。

#### 新設: `src/components/customer/CustomerTopPage.tsx`

`Phase1Screen.tsx`・`CustomersScreen.tsx`(いずれも凍結ファイル、今回の指示範囲に限り
解除)の該当箇所で、従来`<CustomerBottomSheet customer={...} reservation={...} .../>`を
直接開いていたのを`<CustomerTopPage customer={...} reservation={...} .../>`に差し替えた
(propsの形は同一、両ファイルの他の変更は一切していない)。

- 表示: 顧客名、年代(下記参照)、初回問診票サムネイル(タップで拡大モーダル・閉じるボタン付き)、
  「接客ログ / AI Timeline」ボタン、「詳細ページを見る→」ボタン。
- 「接客ログ / AI Timeline」ボタン: 受け取った`customer`/`reservation`をそのまま渡して
  `CustomerBottomSheet`を内部でportal表示する。**CustomerBottomSheet自体には一切手を
  加えていない**(既存機能を何も失わない導線)。
- 「詳細ページを見る→」ボタン: `router.push(`/karte/${customer.id}`)`。「詳細ページ」の
  実体は`KarteCustomerSwitcher.tsx`(`/karte/[customerId]`、2026-09-20新設のiPad専用入口。
  お客様モード⇔スタッフモード(PIN 1234)を同一画面内で切り替える設計)である。CustomerBottomSheet
  の「🗂 iPadカルテ(β)」ボタンが担う別の独立ポータル(`IpadStaffKarteView`単体を直接開く方式)
  とは別物であり、混同しないよう本項に明記する。

**要ユーザー判断・未実装(生年月日・年齢の表示)**: 依頼の「基本情報」には生年月日・年齢の
表示が含まれていたが、調査の結果`brain_customers`には生年月日を保持する列が存在せず、
CSV取込(`salonBoardParser.ts`)ではむしろ生年月日をPII(個人情報)として意図的に除去する方針が
既に取られている(`piiSanitizer.ts`)ことを確認した。この方針を無断で覆すのは今回の指示範囲を
超えると判断し、代わりに既存の`age_group`(年代、例:「30代」)のみを`GET /api/customers/[id]`
経由で取得・表示するに留めた。正確な生年月日・年齢を表示するには、PII除去方針の見直し・
`brain_customers`への`birth_date`列追加・入力導線の新設が別途必要であり、ユーザーの明示的な
判断を仰ぐこと。

#### 新設: 初回問診票の保存(既存写真カルテとは独立)

初回問診票(紙の問診票のスキャン画像1枚、顧客ごとに1件のみ・上書き更新)は、既存の
`brain_customer_photos`(`photo_type` CHECK制約が`before`/`after`/`progress`固定、
`ghostSelection.ts`/`comparisonSelection.ts`と密結合)とは意図的に分離した独立の保存先とした。
写真カルテ側のロジック・DB制約には一切触れていない。

- マイグレーション(レビュー用に作成のみ、**未適用**。顔シェーマ機能新設時と同じ運用で、
  適用は別途明示的な承認を得てから行うこと):
  `supabase/migrations/20260924000000_brain_customers_initial_questionnaire.sql`
  — `brain_customers`に`initial_questionnaire_photo_path`(text)・
  `initial_questionnaire_uploaded_at`(timestamptz)を追加。
- API: `app/api/customers/[id]/initial-questionnaire/route.ts`(GET/PUT新設)。認証・認可は
  既存API(`extractStaffFromRequest`→`canAccessCustomer`)と同一パターン。画像は既存の
  `PHOTO_BUCKET`(`customer-photos`)の`initial-questionnaires/{customerId}/questionnaire.{ext}`
  に**常に上書き保存**(`upsert:true`)し、拡張子が変わった場合のみ旧パスを削除する。
  MIME/サイズ検証は`src/lib/photos/constants.ts`の既存定数をそのまま再利用(この定数ファイル
  自体は変更していない)。
- モーダル新設: `src/components/customer/ipadKarte/InitialQuestionnaireCaptureModal.tsx`。
  依頼では「既存の`IpadPhotoCaptureModal.tsx`から登録・更新可能にする」とあったが、同モーダルは
  ゴースト重ね合わせ・顔検出ガイド・傾きガイド・部位タブ等、前回|今回比較専用の機能と密結合
  しており、そこへ手を加えると`ghostSelection.ts`等の凍結・フラジャイルな既存ロジックに
  影響するリスクがあったため、**`IpadPhotoCaptureModal.tsx`自体は一切変更せず**、撮影/選択→
  プレビュー確認→保存という同じ操作感のみを持つ専用の軽量モーダルを新設する方針に切り替えた
  (低レベル関数`captureFrame.ts`・`fileToWebpBlob.ts`は再利用、配色`PALETTE`・
  `headingFont`は`PhotoCompareKit.tsx`から再利用し見た目を統一)。プレビューobject URLは
  `URL.revokeObjectURL()`で都度解放する(iPadでのメモリ解放対策)。

#### 顔シェーマの表示調整

- `src/components/customer/shared/FacialSchemaKit.tsx`:
  `FACIAL_SCHEMA_TEMPLATE_MARGIN_PERCENT`を12→19に変更(表示スケールは`100-19*2=62%`、
  依頼の「60〜65%程度」の範囲内)。スタッフ編集画面・お客様/前回閲覧の両方に同じ定数を
  経由して適用されるため、2画面で見た目が食い違うことはない。canvas(描画レイヤー・
  ストローク座標の正規化基準)自体は従来通りコンテナ全面のままで変更していない。
- `src/components/customer/guestMode/FacialSchemaViewer.tsx`: お客様モードでの初期折りたたみ
  (`expanded`state、既定`false`)と「顔シェーマを表示する ∨」ボタンを新設。展開後もこれまで
  通りpointerハンドラ・編集UIを一切持たない読み取り専用のまま(元々編集不可だったため、
  折りたたみ以外の変更は無い)。
- スタッフ側(`FacialSchemaSection.tsx`、`IpadStaffKarteView.tsx`経由)のデフォルト展開・
  編集可能という挙動は現状のまま(コード確認のみ、変更していない)。

#### iPadパフォーマンス対策について(判断・見送り)

依頼にあった「Canvas描画イベント(pointermove等)の間引き処理」について、
`useFacialSchemaCanvas.ts`の実装を確認したところ、pointermove自体は既に配列へ座標を
追記するだけの軽い処理であり、実際の再描画は呼び出し側(`FacialSchemaSection.tsx`)の
`requestAnimationFrame`ループ側で既に1フレーム1回に間引かれている設計だった(ファイル冒頭
コメントに明記された既存の意図的な設計)。追加の間引きを入れても効果が薄く、逆にパーム
リジェクション判定(`decidePointerDown`/`decidePointerEnd`、複数回の実機不具合調査を経て
確定した既存ロジック)に影響するリスクがあるため、**このフックへの変更は見送った**。
画像メモリ解放(`URL.revokeObjectURL`)は上記の新設モーダルで徹底した。

#### 検証結果

`npm run typecheck`・`npm run build`ともにパス(既存の無関係な失敗10件は今回の変更前から
存在する別ファイル(e2e仕様・一部ユニットテスト)のものであり、今回の変更による新規エラーは
無い)。`next-env.d.ts`のbuild副作用は確認後に`git checkout`で復元済み。

**実機/E2E未確認**: 上記マイグレーション未適用のため、初回問診票のアップロード・表示は
本番Supabaseに対して実地検証していない(コードレビューレベルの確認に留まる)。顔シェーマの
表示スケール変更・折りたたみボタンもiPad実機(Safari)では未確認。

**この解除は上記(顧客トップページ新設、`Phase1Screen.tsx`/`CustomersScreen.tsx`の遷移先
差し替えのみ、初回問診票の保存機構新設、顔シェーマの表示スケール・お客様モード折りたたみ)に
限る。** 写真4アングル表示・撮影日付表示、生年月日・年齢の実データ表示、
`IpadPhotoCaptureModal.tsx`本体・`ghostSelection.ts`/`comparisonSelection.ts`・
`brain_customer_photos`のphoto_type制約は今回一切変更していない。5タブ構成・TL-5構成・
AI提案の会話トーン・LINE領域・admin領域については引き続き凍結を継続する。

### v1.0.1 着手済み事項（生年月日・年齢の正確表示・PII方針の例外化のみ・2026-09-24ユーザー承認）

直前のエントリで「要ユーザー判断」として保留していた生年月日・年齢の実データ表示について、
現場スタッフ運用上の要望によりユーザーから明示的に承認を得たため、PII除外方針を生年月日に
限り例外化して実装した。電話番号・メール・住所等、生年月日以外のPII除外方針は変更していない。

- **マイグレーション(レビュー用に作成のみ、未適用)**:
  `supabase/migrations/20260924010000_brain_customers_birth_date.sql`
  — `brain_customers`に`birth_date`(date, nullable)を追加。
- **`src/lib/import/salonBoardParser.ts`**: 実際に生年月日を自動スキップしていた箇所
  (`PII_COLUMN_PATTERNS`の`/生年月日/`・`/birthday/i`・`/birth.?date/i`)を削除し、
  `COLUMN_ALIASES.birthDate`(`生年月日`/`誕生日`/`birthdate`/`birthday`/`dob`)を追加。
  行データへの`birthDate`(既存`parseDate()`を再利用、YYYY-MM-DD)の取込も実装した。
  電話・メール・住所のブラックリストは変更していない。
  **重要な事実確認**: `piiSanitizer.ts`自体は生年月日を扱っておらず(会員番号ハッシュ化・
  KEEP列の残存PII走査のみが役割)、実際の除去ロジックは本ファイル(`isPiiColumn`)側に
  あったため、`piiSanitizer.ts`は説明コメントの訂正のみ行い、ロジック変更はしていない。
  `docs/security/PII_POLICY_V1.md`にも追記済み。
- **配線経路**: `SalonBoardImportEngine.ts`(`aggregateCustomers`の後勝ちロジックに
  `birthDate`追加)→`src/types/index.ts`(`SalonBoardRawRow`/`SalonBoardCustomer`/
  `SalonBoardColumnMap`に`birthDate`追加)。ただし**現行の本番CSV取込パイプライン
  (`csvImportPipeline.ts`、売上明細CSVを処理する側)は生年月日に相当する列が
  そもそもCSV仕様に存在しないため、この経路からは自動で入力されない**
  (`csvImportPipeline.ts`自体は無変更。将来、生年月日列を持つ別形式の顧客マスタCSVに
  対応する場合の受け皿として`salonBoardParser.ts`側のみ先行整備した、という位置づけ)。
- **リポジトリ層**: `src/types/riora.types.ts`の`Customer`型に`birthDate?: string | null`
  を追加(既存の大量のテストフィクスチャ(`Customer`型の完全リテラル)を壊さないよう
  あえてoptionalにした)。`CustomerRepo.ts`(`CUSTOMER_COLUMNS`に`birth_date`追加、
  `create`/`patchFromImport`の入力に`birthDate?`追加)・`mappers.ts`
  (`BrainCustomerRow`/`toCustomer`/`toBrainCustomerInsert`)・`interfaces.ts`
  (`ICustomerRepo`)に配線した。`GET /api/customers/[id]`は`customer`オブジェクトを
  そのまま返す既存実装のため、ルート自体の変更は不要だった。
- **`CustomerTopPage.tsx`**: 前回追加した「年代(age_group)」表示を、`birthDate`から
  算出する正確な生年月日・満年齢表示に置き換えた(例:「1987年5月22日（39歳）」)。
  未設定時は「生年月日: 未設定」と表示する。年齢計算は満年齢(誕生日を迎えていなければ
  1引く)。
- **手動入力導線は今回新設していない**: リポジトリ全体を確認したところ、
  `customerRepo.create()`の呼び出し元はCSV取込パイプライン(`csvImportPipeline.ts`・
  `reservationImportPipeline.ts`)のみで、顧客情報を個別に手入力する画面(顧客新規登録・
  編集フォーム)は現状存在しない。そのため、上記CSV経路(将来の顧客マスタCSV対応時)以外で
  `birth_date`を設定する手段は今回実装していない(直接DB更新以外に入力経路が無い状態)。
  必要であれば別途、手動編集UIの新設について指示を仰ぐこと。
- **検証結果**: `npm run typecheck`・`npm run build`ともにパス(既存の無関係な失敗のみ、
  今回の変更による新規エラーは無い)。`next-env.d.ts`のbuild副作用は復元済み。
  マイグレーション未適用のため、実データでの動作(生年月日入りCSVの取込・表示)は
  実地検証していない。

**この解除は上記(`brain_customers.birth_date`カラム追加、`salonBoardParser.ts`の
生年月日PII方針例外化、`CustomerTopPage.tsx`の表示変更、および関連する型・リポジトリ層の
配線)に限る。** 都道府県・市区町村(住所)・LINE User IDに関するPII方針、写真4アングル表示・
撮影日付表示、`IpadPhotoCaptureModal.tsx`本体・`ghostSelection.ts`/`comparisonSelection.ts`・
`brain_customer_photos`のphoto_type制約は今回一切変更していない。5タブ構成・TL-5構成・
AI提案の会話トーン・LINE領域・admin領域については引き続き凍結を継続する。

**追記(2026-09-24): 本番マイグレーション適用・デプロイ完了。** 上記の
`20260924000000_brain_customers_initial_questionnaire.sql`・
`20260924010000_brain_customers_birth_date.sql`をユーザー承認のうえ本番Supabase
(ohszxgajckzphhfhdrsv)へ適用し、`git push`・Vercel本番デプロイ(commit `b729582`)まで
完了した。適用後のセキュリティアドバイザーに新規の指摘は無い(既存の無関係な指摘のみ)。

### v1.0.1 着手済み事項（`/karte`トップ画面の顧客タップ導線修正のみ・2026-09-24ユーザー承認）

上記「顧客トップページ新設」の対応漏れの修正。「本日の予約」「顧客タブ」
(Phase1Screen.tsx/CustomersScreen.tsx)は`CustomerTopPage`経由に変更済みだったが、
`/karte`専用入口(`KarteEntryScreen.tsx`、PHASE IPAD-KARTE-ENTRY-1)は別画面のため
見落としており、顧客タップで`CustomerTopPage`を経由せず直接`/karte/[customerId]`へ
遷移する不具合が本番で発生していた。ユーザーからの本番動作確認報告を受けて修正した。

- **`src/components/karte/KarteEntryScreen.tsx`**: `openCustomer(customerId)`
  (`router.push('/karte/${customerId}')`直呼び)を廃止し、`CustomerTopPage`をportal表示する
  `selected` stateに置き換えた。検索結果(`CustomerRow`)・本日の予約
  (`ReservationWithBrainCustomer`)それぞれについて、`CustomersScreen.tsx`/`Phase1Screen.tsx`
  と同型の`toCustomer*/toReservation*`変換関数をこのファイル専用に複製した(既存ファイルは
  無変更、このプロジェクトで既に採られている複製パターンを踏襲)。`/karte/[customerId]`への
  遷移は`CustomerTopPage`内の「詳細ページを見る→」ボタンが担う(変更不要、既存のまま)。
  `customer_type`のstring→`CustomerType`変換は`Phase1Screen.tsx`の
  `(bc.customer_type as CustomerType) || 'VIP型'`と同じ既存パターンを踏襲した。
- **検証結果**: `npm run typecheck`・`npm run build`ともにパス(既存の無関係な失敗のみ)。
  `next-env.d.ts`のbuild副作用は復元済み。iPad実機での再現確認は未実施(コードレベルの
  修正のみ、ユーザー側での本番再確認を推奨)。

**この解除は`KarteEntryScreen.tsx`の顧客タップ導線修正に限る。** `CustomerTopPage.tsx`・
`CustomerBottomSheet.tsx`・`Phase1Screen.tsx`・`CustomersScreen.tsx`自体には一切触れていない。

### v1.0.1 着手済み事項（スマホアプリ側と`/karte`専用領域の完全分離・CustomerTopPage表示調整のみ・2026-09-24ユーザー承認）

直前の2エントリで「本日の予約」「顧客タブ」(Phase1Screen.tsx/CustomersScreen.tsx、
スマホでも使う一般的なスタッフアプリ画面)にも`CustomerTopPage`経由の遷移を導入していたが、
ユーザーから「スマホアプリとカルテ(`/karte`)は別物であるべき」との明確な指摘を受け、
以下の通り切り分けた。

- **`src/components/phase1/Phase1Screen.tsx`・`CustomersScreen.tsx`**: お客様名タップ時の
  遷移を、`CustomerTopPage`経由から**元通りCustomerBottomSheetを直接開く挙動に完全に戻した**
  (import・JSXともにcommit cc0ef4d以前の状態へ復元)。スマホアプリ側には顧客トップページ・
  `/karte/[customerId]`への自動遷移は一切含まれない。
- **`src/components/karte/KarteEntryScreen.tsx`**: 変更なし。「顧客トップページ→詳細ページ
  (`/karte/[customerId]`)」の導線は`/karte`専用入口(このファイル)のみに限定される
  (ファイル冒頭コメントに経緯を追記)。
- **`src/components/customer/CustomerTopPage.tsx`**(`/karte`専用領域でのみ使用):
  - 「接客ログ / AI Timeline」ボタン(CustomerBottomSheetへの導線)を削除した。
    `showBottomSheet` state・関連render・`CustomerBottomSheet`のimportも合わせて削除
    (呼び出し元が無くなったため。CustomerBottomSheet自体は無改修)。
  - 敬称を「さま」(ひらがな)から「様」(漢字)に変更した(例:「杉原 良和 様」)。
- **検証結果**: `npm run typecheck`・`npm run build`ともにパス(既存の無関係な失敗のみ)。
  `next-env.d.ts`のbuild副作用は復元済み。

**この解除は上記(スマホアプリ側2ファイルの遷移復元、CustomerTopPage.tsxのボタン削除・
敬称変更)に限る。** `KarteEntryScreen.tsx`・`CustomerBottomSheet.tsx`自体には触れていない。

#### 生年月日CSV取込の再確認結果(要ユーザー判断・未対応)

ユーザー依頼により、`salonBoardParser.ts`のPII方針例外化(2026-09-24)が実際に
`brain_customers.birth_date`へ取り込まれる状態になっているか再確認した。**結論:
現状のCSV取込では生年月日は一切取り込まれない。** 前回のCLAUDE.md記載
(「将来の顧客マスタCSV対応時の受け皿として先行整備」)は不正確だったため訂正する。

- 実際に本番で使われているCSV取込は`app/api/admin/csv/import/route.ts`
  → `csvImportPipeline.ts`(`runImportPipeline`)の1系統のみ。これは「売上明細CSV」
  (`salonboard_utf8.csv`と同形式、列: 会計日/会計時間/お客様名/お客様番号/性別/
  新規再来...)を処理し、`customerRepo.create()`/`patchFromImport()`経由で
  `brain_customers`へ書き込む。**このCSV形式には生年月日に相当する列がそもそも
  存在しない**(`csvImportPipeline.ts`冒頭コメントに元々明記されていた事実)。
- 一方、生年月日PII例外化を行った`salonBoardParser.ts`は、`SalonBoardImportEngine.ts`
  (`aggregateCustomers`)・`SalonBoardSaveEngine.ts`(`saveSalonBoardImport`、
  **legacy `customers`テーブルへ書き込む別系統**、`brain_customers`ではない)からのみ
  参照されており、**この一連(`SalonBoardImportEngine.ts`/`SalonBoardSaveEngine.ts`)を
  呼び出すAPIルートがリポジトリ内に存在しない**(grep確認済み、`app/`配下に呼び出し元0件)。
  実質的に現在使われていないコード経路であり、仮に呼ばれても書き込み先が
  `brain_customers`ではなく別テーブルのため、いずれにせよ生年月日は反映されない。
- **つまり現時点で`brain_customers.birth_date`を入力する経路はCSV取込・画面入力
  いずれも存在せず、直接DB操作以外に設定手段が無い。** CSV経由で生年月日を取り込みたい
  場合は、(a)売上明細CSVとは別に生年月日を含む顧客マスタCSVを用意しその専用取込を
  新設するか、(b)顧客編集画面(現状存在しない)を新設するか、方針決定が必要
  (今回はコード変更をせず、事実確認のみ実施)。

### v1.0.1 着手済み事項（生年月日の手動入力・年齢の自動計算表示のみ・2026-09-24ユーザー承認）

直前のエントリで判明した「生年月日の入力経路が存在しない」ギャップへの対応。SalonBoardの
CSV出力に生年月日が無いため、上記(b)の方針(顧客トップページからの手動入力)を実装した。

- **`src/lib/customer/birthDate.ts`(新規)**: 生年月日まわりのロジックを集約する共通
  ユーティリティ。`parseFlexibleBirthDateInput()`(「1986/05/30」「1986-5-30」
  「1986年5月30日」等の表記ゆれをISO(YYYY-MM-DD)へ正規化、無効日・未来日はnull)・
  `calculateAge()`・`formatBirthDateJapanese()`・`formatBirthDateSlash()`。
  従来`CustomerTopPage.tsx`にあった`calculateAge`/`formatBirthDateJapanese`の重複定義は
  ここへ統合し、`CustomerTopPage.tsx`側は削除した。
- **`app/api/customers/[id]/birth-date/route.ts`(新規、PATCHのみ)**: `goal/route.ts`と
  同一の認証・アクセス制御パターン(`extractStaffFromRequest`+`canAccessCustomer`)。
  入力文字列を`parseFlexibleBirthDateInput()`でパースし、失敗時は400
  (`invalid_birth_date_format`)を返す。空文字/nullは「未設定に戻す」としてNULLへ正規化。
  GETは追加していない(既存の`GET /api/customers/[id]`が`birthDate`を含めて返すため不要)。
- **`CustomerTopPage.tsx`**: 生年月日表示の右に鉛筆アイコンを追加し、タップで
  テキスト入力(コピペ対応・`inputMode="numeric"`)→保存/キャンセルのインライン編集UIを
  実装。保存時は上記PATCHを呼び、成功時は表示を即座に更新する。
- **`CustomerModeView.tsx`・`IpadStaffKarteView.tsx`**: いずれもヘッダーの
  「{customerName}様」表示に`（38歳）`のような年齢を追記した(**表示のみ・編集UIは
  無し**。入力は`CustomerTopPage.tsx`に一本化)。取得は既存の`GET /api/customers/[id]`を
  この画面専用に直接呼ぶ自己完結fetch(`FacialSchemaViewer.tsx`と同じパターン)で、
  `useCustomerModeData`/`useIpadKarteData`という大きな共有フックには一切手を加えていない。
- **検証結果**: `npm run typecheck`・`npm run build`ともにパス(既存の無関係な失敗のみ、
  新規APIルート`/api/customers/[id]/birth-date`もビルド出力に含まれることを確認)。
  `next-env.d.ts`のbuild副作用は復元済み。実機での実際の保存・コピペ動作は未検証。

**この解除は上記(生年月日の手動入力UI新設・共通ユーティリティ新設・年齢表示追加)に限る。**
CSV取込(`csvImportPipeline.ts`・`salonBoardParser.ts`)には一切触れていない。

### v1.0.1 着手済み事項（KarteMemoSection.tsx「カルテメモを追加」欄の常時展開化のみ・2026-09-24ユーザー承認）

カルテメモの新規追加欄がデフォルトで折りたたまれており(「＋カルテメモを追加」ボタンを
押すまで入力欄が出ない)、ユーザーから「折りたたまなくて良い」との指摘を受け修正した。

- `adding` stateの初期値を`false`→`true`に変更(初期表示から直接入力欄が見える)。
- `handleAdd()`成功時の`setAdding(false)`を削除し、保存後も入力欄を開いたままにした
  (連続してメモを書き込める。手動で閉じる「キャンセル」ボタン自体は残しているため、
  閉じたい場合の操作は引き続き可能)。
- 「前回の記録を見る」サブ項目(`previousOpen`、既にデフォルト展開)・一覧表示ロジックには
  一切触れていない。
- **検証結果**: `npm run typecheck`・`npm run build`ともにパス(既存の無関係な失敗のみ)。

**この解除は上記`adding`初期値・保存後の挙動変更に限る。**

### v1.0.1 着手済み事項（SalonBoard「お客様情報詳細」テキスト貼り付け取込機能の新設のみ・2026-09-24ユーザー承認）

SalonBoardのCSV出力に生年月日等が無い問題(前々回のエントリ参照)への追加対応。
CSV経由ではなく、SalonBoardの「お客様情報詳細」ページをテキストとしてそのまま
コピペし、既存顧客(顧客トップページで選択中の顧客)へ一括反映する方式を新設した。
**新規顧客の作成は対象外(既存顧客への情報補完のみ)。** 電話番号は個人情報方針により
解析・保存いずれも行わない(ユーザー承認済み)。

- **マイグレーション(レビュー用に作成のみ→ユーザー承認のうえ本番適用済み)**:
  `supabase/migrations/20260924020000_brain_customers_salonboard_import_fields.sql`
  — `brain_customers`に`salonboard_visit_count`(integer)・`postcard_consent`(text)を追加。
  「初回来店日」(`first_visit_date`)・「来店きっかけ」(`acquisition_channel`)は既存カラムを
  再利用した(重複追加していない)。`salonboard_visit_count`は`brain_visits`由来の実測
  集計値とは別物(SalonBoard「来店回数」欄の生値)であることを明示するため、あえて
  汎用の"visit_count"にしていない。
- **`src/lib/customer/salonBoardTextParser.ts`(新規)**: 行ベースの緩いラベル一致で
  「氏名(漢字)」「誕生日」「初回来店日」「来店回数」「来店きっかけ」「はがき送付許諾」を
  抽出する(区切り文字の有無を問わない)。日付は既存の`parseFlexibleBirthDateInput()`
  (birthDate.ts)を再利用。「-」等の空値表現は空として扱う。**電話番号用の正規表現は
  一切実装していない**(パーサー自体が電話番号を認識しない設計)。氏名は確認表示専用
  (`brain_customers.name`へは書き込まない、既存の顧客名を誤って上書きするリスクを
  避けるため)。
- **`app/api/customers/[id]/import-salonboard-text/route.ts`(新規、PATCHのみ)**:
  `goal/route.ts`と同一の認証パターン。貼り付けテキストに含まれていない項目は
  更新対象から除外する(見つかった項目だけを上書き、既存値をnull化しない)。
  該当項目が1つも見つからなければ400(`no_recognizable_fields`)を返す。
- **`src/components/customer/SalonBoardImportModal.tsx`(新規)**: 「サロンボード情報を
  取り込む」ボタン(顧客トップページの基本情報カード内)から開くモーダル。テキスト
  貼り付け→取込→検出項目のプレビュー表示という流れ。取込成功時は
  `CustomerTopPage.tsx`側の生年月日等の表示を再取得し、年齢を即座に再計算する。
- **リポジトリ層**: `CustomerRepo.ts`(`CUSTOMER_COLUMNS`に2カラム追加)・
  `mappers.ts`(`BrainCustomerRow`/`toCustomer`)・`riora.types.ts`(`Customer`型に
  `salonboardVisitCount`/`postcardConsent`をoptional追加、既存テストフィクスチャ保護)。
- **検証結果**: `npm run typecheck`・`npm run build`ともにパス(既存の無関係な失敗のみ、
  新規APIルートもビルド出力に含まれることを確認)。マイグレーション適用後のセキュリティ
  アドバイザーに新規の指摘は無い。`next-env.d.ts`のbuild副作用は復元済み。実機での
  実際のコピペ動作(SalonBoard実データでの解析精度)は未検証。

**この解除は上記(SalonBoardテキスト貼り付け取込機能の新設・関連カラム追加)に限る。**
CSV取込パイプライン(`csvImportPipeline.ts`・`salonBoardParser.ts`)・電話番号PII方針には
一切触れていない。

### v1.0.1 着手済み事項（SalonBoard取込導線をスタッフモード側へ移設のみ・2026-09-24ユーザー承認）

直前のエントリで顧客トップページ(`CustomerTopPage.tsx`)に置いたSalonBoard取込機能
について、「お客様と一緒に見る画面にはがき送付許諾・来店きっかけ等の内部情報を
出したくない」というユーザー指摘を受け、配置を変更した。

- **`src/components/customer/CustomerTopPage.tsx`**: 「サロンボード情報を取り込む」
  ボタン・初回来店日/来店回数/来店きっかけ/はがき送付許諾の表示・`SalonBoardImportModal`
  の呼び出し・関連state(`firstVisitDate`/`salonboardVisitCount`/`acquisitionChannel`/
  `postcardConsent`/`showSalonBoardImport`)を全て削除した。生年月日の表示・鉛筆編集
  機能はこのファイルに残したまま(ユーザー指示は「はがき送付許諾・来店きっかけ等の
  内部情報」が対象で、生年月日/年齢は「お客様に見せても問題ない項目」として明示的に
  対象外とされたため)。
- **`src/components/customer/ipadKarte/IpadStaffKarteView.tsx`(PIN保護されたスタッフ
  モード)**: 「📋 サロンボード情報」Cardを新設し、`CustomerStatusPanel`(顧客ステータス)
  の直下(ユーザー指定の配置)に追加した。初回来店日/来店回数/来店きっかけ/はがき送付
  許諾の表示と「サロンボード情報を取り込む」ボタン(`SalonBoardImportModal`を開く)を
  ここに集約した。年齢表示は既存のヘッダー表示(`{customerName}様（38歳）`)をそのまま
  維持(変更なし)。`fetchCustomerDetail`を`useCallback`化し、取込成功時
  (`onImported`)にこの画面の表示を再取得する。
- **`SalonBoardImportModal.tsx`本体・`import-salonboard-text`API・パーサー・
  マイグレーションには一切手を加えていない**(呼び出し元(props: `customerId`/
  `onClose`/`onImported`)が変わっただけ)。
- **お客様モード(`CustomerModeView.tsx`)**: 元々SalonBoard関連の表示は一切実装して
  いなかった(今回変更なし)。年齢表示のみ既存通り残る。
- **検証結果**: `npm run typecheck`・`npm run build`ともにパス(既存の無関係な失敗のみ)。

**この解除は上記(SalonBoard取込導線の`CustomerTopPage.tsx`→`IpadStaffKarteView.tsx`
への移設)に限る。** `SalonBoardImportModal.tsx`本体・API・パーサー・マイグレーション・
`CustomerModeView.tsx`には一切触れていない。

## v1凍結フェーズ 安全制御ルール（最優先・常時適用）

詳細・根拠・影響範囲は `docs/V1_FREEZE_SAFETY_RULES.md` を参照。ここには実行を縛る要約のみ記す。

### 承認なしに実行してはいけないこと
- `src/components/line/**`、`app/line/**`、`app/api/line/**`、`app/api/line-queue/**`、
  `src/lib/line/**`（LINE画面・外部連携）の変更
- `app/admin/**`（管理者アプリ）の変更
- 直前の指示で明示的に列挙されていないファイルの変更（「ついでに」「関連するので」を含む）
- 「軽微だから」「1件だけだから」という理由での部分実行
- v1が凍結可能かどうかの判断そのもの

これらは指示があっても、ユーザーの明示的な実行承認（具体的なYes/実行依頼）を得るまで着手しない。

### 必ず停止して待機する状態
- ユーザーに質問を送り、回答を待っている間
- Yes/No・2択・3択などの分岐が未確定の間
- 対象スコープ（どこまで触ってよいか）が確定していない間

**この間は、タイムアウトや無応答を理由に「推奨案で進める」等の先行実行をしない。** 応答が
なければ待機を続けるか、その分岐に触れない範囲の別タスクに切り替える。

### 禁止事項
- 無応答を理由とした先行実行
- 推測によるユーザー意図の補完・実装
- ユーザー未確認でのロールバック（`git checkout` / `git reset` 等の破棄的操作も含む。
  「明らかに戻すべき」と思える場合でも実行前に確認を取る）
- 依頼範囲を超えた「勝手なリファクタリング」（見つけたら報告し、修正は指示を待つ）

### 既知の未承認差分（解消済み）
`src/components/line/LineApprovalScreen.tsx` の未承認編集は、2026-07-03にユーザー承認の上で
`git checkout`によりロールバック済み。LINE領域は現在クリーン（無変更）な状態。経緯の詳細は
`docs/V1_FREEZE_SAFETY_RULES.md` の「今回防止された事故のログ」を参照。

## LINE CRM 運用制約（2026-08確定・常時適用）

詳細は `docs/LINE_CRM_CURRENT_STATE_2026-08.md` を参照。

- Riora専用のLINE Channel Access TokenがProductionに正しく設定されるまで、LINE CRMの本番化・
  LINE実送信の実機検証を行わない（現在Productionに設定されているのは別法人のトークンのため）。
- AI生成LINEの自動送信は、生成精度が十分に改善されるまで実装・有効化しない
  （AI下書き→スタッフ確認・修正→スタッフの明示的な送信操作→1件送信、の運用を維持する）。

## スタッフ向け使い方ガイド 更新チェック（新機能追加時・常時適用）

スタッフが実際に読むガイドの実体は `app/menu/guide/page.tsx`（静的JSXにハードコード、DB・APIなし）。
2026-08-09に6並列コード調査に基づき全面書き直し済み（詳細は当該セッションの会話・コミット参照）。
`docs/Riora_スタッフアプリ使い方ガイド_設計書_*.md`・`docs/GUIDE_STAFF_APP_v1.1.md` は当時の
構想・監査記録であり、いずれも現行版より古い。**参照するのは常にコードそのものであり、これら
設計書ではない。**

### 以下に該当する変更を行った・行う場合は、Guideとの整合性チェックを行う
- 5タブ構成（今日/顧客/メモ/マイページ/設定）の追加・削除・名称変更・遷移先変更
- 今日タブ（ブリーフィング・サマリー・「今日、気をつけること」等）の表示項目・集計ロジックの変更
- CustomerBottomSheet内のセクション追加・削除・仕様変更（禁忌事項・Customer Memory・肌タグ・
  カルテ取込・AI Timeline・接客記録など）
- AI提案系（次回来店提案・ホームケア提案・関連ブログ・会話のきっかけ・提案履歴・今日の接客
  ポイント）の新設・ロジック変更・LLM利用有無の変更
- LINE関連（メッセージ生成の種別追加、送信フロー、送信履歴・送信済み表示、重複防止の仕組み）
  の変更。特に「自動送信」に近づく変更は上記のLINE CRM運用制約と矛盾しないか要確認
- 通知の種類追加・発火条件変更（誕生日/記念日/来店リマインド/ホームケア3タッチ/そろそろ等）
- マイページ「そろそろの方」の判定ロジック（対象商品・日数の目安・抑制条件）の変更
- 設定タブの中身の変更（現状はメニュー実績閲覧＋ログアウトのみ、個別設定機能なし）

### 実施手順
1. 対象コードを直接読んで現状を確認する（Guideの既存記述や設計書からの推測は禁止）。
2. Guideの該当箇所が実装とズレていないか確認する。ズレがあれば、その内容をユーザーに報告する。
3. Guide更新はユーザーの明示的な承認を得てから行う（機能実装のついでに無断でGuideも書き換えない）。
4. Guideに書く内容は「実装されている事実」のみ。未実装・検討中の機能、「自動で決定/算出する」
   「正確に◯◯する」といった断定表現、実装より広い範囲を示唆する表現（例:「今週の予約」と
   書きつつ実際は当日・翌日のみ集計、等）は書かない。数値ロジック（そろそろの日数計算等）を
   記載する場合は、正確な計算式ではなく実装と矛盾しないスタッフ向けの平易な説明に言い換える。

---

## プロジェクト概要

Riora OS は銀座高級エステサロン向けの「接客OS」。スタッフアプリ（顧客対応・接客支援）と
管理者アプリ（経営分析）を明確に分離する設計方針を持つ（詳細はスタッフアプリv1再設計に関する
会話履歴・メモリを参照）。

- スタッフアプリ: 事実情報のみ表示。スコア・ランク・順位・確率などの評価系UIは禁止。
- 管理者アプリ (`app/admin/**`): 経営分析・スタッフ比較・ランキング等はこちらに集約する。
